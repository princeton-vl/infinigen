import math
from typing import NamedTuple

import numpy as np
import procfunc as pf
from procfunc.nodes import types as t

from infinigen_v2.generators.shaders.util.coord import coord_warp


class SandstoneDisplacementLayerResult(NamedTuple):
    vector: t.SocketOrVal[pf.Vector]
    height: pf.ProcNode[float]
    noise: pf.ProcNode[float]


@pf.nodes.node_function
def sandstone_displacement_layer(
    vector: t.SocketOrVal[pf.Vector],
    base_height: t.SocketOrVal[float] = 0.0,
    noise_type: t.SocketOrVal[float] = 1.0,
    height: t.SocketOrVal[float] = 1.0,
    height_offset: t.SocketOrVal[float] = 0.0,
    mix_mode: t.SocketOrVal[float] = 0.0,
    size: t.SocketOrVal[float] = 5.0,
    stretch: t.SocketOrVal[float] = 0.0,
    rotation: t.SocketOrVal[pf.Vector] = (0.0, 0.0, 0.0),
    w: t.SocketOrVal[float] = 0.0,
    detail: t.SocketOrVal[float] = 1.0,
    spread: t.SocketOrVal[float] = 0.0,
    smoothness: t.SocketOrVal[float] = 0.0,
    distortion_strength: t.SocketOrVal[float] = 1.0,
    invert: t.SocketOrVal[float] = 0.0,
) -> SandstoneDisplacementLayerResult:
    # Code generated by procfunc v0.4.0

    height_factor = (mix_mode - 1.0) - 1.0

    noise_value_factor = noise_type - 1.0
    noise_vector_size = size * 0.1

    coord_warp_result = coord_warp(
        vector=vector,
        size=noise_vector_size,
        strength=distortion_strength * 2.8,
        detail=detail / 2.0,
    )
    coord_warp_result_1 = coord_warp(
        vector=coord_warp_result.vector,
        size=noise_vector_size * 3.0,
        strength=distortion_strength * 1.8,
        detail=detail,
    )

    noise_value_vector_1 = pf.nodes.math.vector_rotate_euler(
        vector=coord_warp_result_1.vector,
        rotation=rotation,
    )
    noise_value_vector = pf.nodes.func.combine_xyz(x=1.0, y=1.0, z=stretch)
    noise_value_4 = pf.nodes.shader.voronoi(
        vector=noise_value_vector_1 / noise_value_vector,
        w=w,
        scale=5.0 / size,
        detail=detail / 4.0,
        normalize=True,
        voronoi_dimensions="4D",
    )
    noise_value_3 = pf.nodes.shader.noise(
        vector=noise_value_vector_1 / noise_value_vector,
        w=w,
        scale=5.0 / size,
        detail=detail,
        noise_dimensions="4D",
    )
    noise_value_2 = pf.nodes.func.mix(
        factor=noise_value_factor,
        a=noise_value_4.distance,
        b=noise_value_3.fac,
    )
    noise_value_1 = pf.nodes.shader.voronoi_distance(
        vector=noise_value_vector_1 / noise_value_vector,
        w=w,
        scale=5.0 / size,
        detail=detail / 4.0,
        normalize=True,
        voronoi_dimensions="4D",
    )
    noise_value = pf.nodes.func.mix(
        factor=noise_value_factor - 1.0, a=noise_value_2, b=noise_value_1
    )
    noise_from_min_1 = 1.0 - smoothness
    noise_from_min = spread - 0.0001
    noise_1 = pf.nodes.func.map_range(
        value=noise_value,
        from_min=noise_from_min_1 * noise_from_min,
        from_max=spread,
        to_min=1.0,
        to_max=0.0,
    )
    noise = pf.nodes.func.mix(factor=invert, a=noise_1, b=1.0 - noise_1)

    height_6 = noise * (height * 0.01)
    height_5 = pf.nodes.func.mix(
        factor=mix_mode - 1.0,
        a=(height_6 + height_offset) + base_height,
        b=base_height - (height_6 + height_offset),
    )
    height_4 = pf.nodes.math.maximum(a=height_6 + height_offset, b=base_height)
    height_3 = pf.nodes.func.mix(factor=height_factor, a=height_5, b=height_4)
    height_2 = pf.nodes.math.minimum(a=height_6 + height_offset, b=base_height)
    height_1 = pf.nodes.func.mix(factor=height_factor - 1.0, a=height_3, b=height_2)
    return SandstoneDisplacementLayerResult(
        vector=vector,
        height=height_1,
        noise=noise,
    )


class SandstoneColorLayerResult(NamedTuple):
    vector: t.SocketOrVal[pf.Vector]
    color: pf.ProcNode[pf.Color]
    noise: pf.ProcNode[float]


@pf.nodes.node_function
def sandstone_color_layer(
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color] = pf.Color((0.5, 0.5, 0.5)),
    noise_type: t.SocketOrVal[float] = 1.0,
    texture_color: t.SocketOrVal[pf.Color] = pf.Color((0.5, 0.5, 0.5)),
    mix_mode: t.SocketOrVal[float] = 0.0,
    size: t.SocketOrVal[float] = 5.0,
    stretch: t.SocketOrVal[float] = 0.0,
    rotation: t.SocketOrVal[pf.Vector] = (0.0, 0.0, 0.0),
    w: t.SocketOrVal[float] = 0.0,
    detail: t.SocketOrVal[float] = 1.0,
    spread: t.SocketOrVal[float] = 0.0,
    smoothness: t.SocketOrVal[float] = 0.0,
    distortion_strength: t.SocketOrVal[float] = 1.0,
    invert: t.SocketOrVal[float] = 0.0,
    mask: t.SocketOrVal[float] = 1.0,
) -> SandstoneColorLayerResult:
    # Code generated by procfunc v0.4.0

    color_factor = (mix_mode - 1.0) - 1.0

    noise_value_factor = noise_type - 1.0
    noise_vector_size = size * 0.1

    coord_warp_result = coord_warp(
        vector=vector,
        size=noise_vector_size,
        strength=distortion_strength * 2.8,
        detail=detail / 2.0,
    )
    coord_warp_result_1 = coord_warp(
        vector=coord_warp_result.vector,
        size=noise_vector_size * 3.0,
        strength=distortion_strength * 1.8,
        detail=detail,
    )

    noise_vector = pf.nodes.func.combine_xyz(x=1.0, y=1.0, z=stretch)
    noise_value_vector = pf.nodes.math.vector_rotate_euler(
        vector=coord_warp_result_1.vector / noise_vector,
        rotation=rotation,
    )
    noise_value_4 = pf.nodes.shader.voronoi(
        vector=noise_value_vector,
        w=w,
        scale=5.0 / size,
        detail=detail / 4.0,
        normalize=True,
        voronoi_dimensions="4D",
    )
    noise_value_3 = pf.nodes.shader.noise(
        vector=noise_value_vector,
        w=w,
        scale=5.0 / size,
        detail=detail,
        noise_dimensions="4D",
    )
    noise_value_2 = pf.nodes.func.mix_rgb(
        factor=noise_value_factor,
        a=noise_value_4.distance.astype(dtype=pf.Color),
        b=noise_value_3.fac.astype(dtype=pf.Color),
    )
    noise_value_1 = pf.nodes.shader.voronoi_distance(
        vector=noise_value_vector,
        w=w,
        scale=5.0 / size,
        detail=detail / 4.0,
        normalize=True,
        voronoi_dimensions="4D",
    )
    noise_value = pf.nodes.func.mix_rgb(
        factor=noise_value_factor - 1.0,
        a=noise_value_2,
        b=noise_value_1.astype(dtype=pf.Color),
    )
    noise_from_min_1 = 1.0 - smoothness
    noise_from_min = spread - 0.0001
    noise_1 = pf.nodes.func.map_range(
        value=noise_value.astype(dtype=float),
        from_min=noise_from_min_1 * noise_from_min,
        from_max=spread,
        to_min=1.0,
        to_max=0.0,
    )
    noise = pf.nodes.func.mix(factor=invert, a=noise_1, b=1.0 - noise_1)

    color_7 = pf.nodes.func.mix_rgb(factor=noise, a=base_color, b=texture_color)
    color_6 = pf.nodes.func.mix_rgb(
        factor=noise,
        a=base_color,
        b=texture_color,
        blend_type="MULTIPLY",
    )
    color_5 = pf.nodes.func.mix_rgb(factor=mix_mode - 1.0, a=color_7, b=color_6)
    color_4 = pf.nodes.func.mix_rgb(
        factor=noise,
        a=base_color,
        b=texture_color,
        blend_type="SOFT_LIGHT",
    )
    color_3 = pf.nodes.func.mix_rgb(factor=color_factor, a=color_5, b=color_4)
    color_2 = pf.nodes.func.mix_rgb(
        factor=noise, a=base_color, b=texture_color, blend_type="OVERLAY"
    )
    color_1 = pf.nodes.func.mix_rgb(factor=color_factor - 1.0, a=color_3, b=color_2)
    color = pf.nodes.func.mix_rgb(factor=mask, a=base_color, b=color_1)
    return SandstoneColorLayerResult(
        vector=vector,
        color=color,
        noise=noise,
    )


def _stone_red_sandstone_distribution(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    mask_layer2: t.SocketOrVal[float | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    mask_layer3: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    smoothness_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
    spread_disp3: t.SocketOrVal[float | None] = None,
    smoothness_disp3: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.4, 1.5)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.15, 0.75)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.3, 1.0)
    if mask_layer2 is None:
        mask_layer2 = pf.random.uniform(rng, 0.2, 0.95)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.1, 0.8)
    if mask_layer3 is None:
        mask_layer3 = pf.random.uniform(rng, 0.15, 0.7)
    if roughness is None:
        roughness = pf.random.uniform(rng, 0.88, 0.98)
    if specular_ior_level is None:
        specular_ior_level = pf.random.uniform(rng, 0.02, 0.08)
    if smoothness_disp1 is None:
        smoothness_disp1 = pf.random.uniform(rng, 0.15, 0.65)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 0.8, 2.5)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.4, 0.9)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 1.2, 3.0)
    if spread_disp3 is None:
        spread_disp3 = pf.random.uniform(rng, 0.4, 1.2)
    if smoothness_disp3 is None:
        smoothness_disp3 = pf.random.uniform(rng, 0.4, 0.9)

    base_color_hsv = pf.random.uniform(
        rng,
        np.array([0.005, 0.75, 0.12]),
        np.array([0.08, 0.98, 0.28]),
    )
    base_color = pf.color.hsv_to_rgba(base_color_hsv)

    h_perturb = (pf.random.beta(rng, 0.4, 0.9) - 0.3) * 0.16
    s_perturb = pf.random.uniform(rng, -0.175, 0.175)
    v_perturb = pf.random.uniform(rng, -0.112, 0.168)
    texture_color_1_hsv = base_color_hsv + np.array([h_perturb, s_perturb, v_perturb])
    texture_color_1 = pf.color.hsv_to_rgba(texture_color_1_hsv)
    sandstone_color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_1,
        mix_mode=1.0,
        size=1.0,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    h_perturb = (pf.random.beta(rng, 0.5, 0.5) - 0.5) * 0.20
    s_perturb = pf.random.uniform(rng, -0.172, 0.258)
    v_perturb = pf.random.uniform(rng, -0.135, 0.135)
    texture_color_2_hsv = base_color_hsv + np.array([h_perturb, s_perturb, v_perturb])
    texture_color_2 = pf.color.hsv_to_rgba(texture_color_2_hsv)
    color_layer_result_1 = sandstone_color_layer(
        vector=sandstone_color_layer_result.vector,
        base_color=sandstone_color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_2,
        mix_mode=1.0,
        size=0.5,
        stretch=1.0,
        w=8.8,
        detail=8.0,
        spread=spread_layer2,
        smoothness=1.0,
        mask=mask_layer2,
    )
    h_perturb = (pf.random.beta(rng, 0.6, 0.6) - 0.5) * 0.12
    s_perturb = pf.random.uniform(rng, -0.175, 0.175)
    v_perturb = pf.random.uniform(rng, -0.168, 0.112)
    texture_color_3_hsv = base_color_hsv + np.array([h_perturb, s_perturb, v_perturb])
    texture_color_3 = pf.color.hsv_to_rgba(texture_color_3_hsv)
    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_3,
        mix_mode=2.0,
        size=0.05,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=1.0,
        mask=mask_layer3,
    )

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_2.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    sandstone_displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        noise_type=2.0,
        height=0.08,
        mix_mode=1.0,
        size=0.01,
        stretch=4.0,
        rotation=(0.0, math.pi / 2, 0.0),
        detail=4.0,
        spread=0.5,
        smoothness=smoothness_disp1,
        distortion_strength=distortion_strength_disp1,
    )

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=sandstone_displacement_layer_result.vector,
        base_height=sandstone_displacement_layer_result.height,
        noise_type=2.0,
        height=0.12,
        mix_mode=1.0,
        size=0.01,
        stretch=100.0,
        rotation=(math.pi / 2, -1.4678, math.pi / 2),
        detail=6.0,
        spread=0.5,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )
    displacement_layer_result_2 = sandstone_displacement_layer(
        vector=displacement_layer_result_1.vector,
        base_height=displacement_layer_result_1.height,
        noise_type=2.0,
        height=0.1,
        mix_mode=1.0,
        size=0.02,
        stretch=1.0,
        detail=4.0,
        spread=spread_disp3,
        smoothness=smoothness_disp3,
    )
    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_2.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


# def sandstone_displacement_layer_distribution(
#     rng: pf.RNG,
#     vector: t.SocketOrVal[pf.Vector],
#     base_height: t.SocketOrVal[float | None] = None,
#     noise_type: t.SocketOrVal[float | None] = None,
#     height: t.SocketOrVal[float | None] = None,
#     height_offset: t.SocketOrVal[float | None] = None,
#     mix_mode: t.SocketOrVal[float | None] = None,
#     size: t.SocketOrVal[float | None] = None,
#     stretch: t.SocketOrVal[float | None] = None,
#     rotation: t.SocketOrVal[pf.Vector | None] = None,
#     w: t.SocketOrVal[float | None] = None,
#     detail: t.SocketOrVal[float | None] = None,
#     spread: t.SocketOrVal[float | None] = None,
#     smoothness: t.SocketOrVal[float | None] = None,
#     distortion_strength: t.SocketOrVal[float | None] = None,
#     invert: t.SocketOrVal[float | None] = None,
# ):
#     if base_height is None:
#         base_height = 0.0
#     if noise_type is None:
#         noise_type = pf.random.uniform(rng, 1.0, 2.0)
#     if height is None:
#         height = pf.random.uniform(rng, 0.08, 0.7)
#     if height_offset is None:
#         height_offset = 0.0
#     if mix_mode is None:
#         mix_mode = pf.random.uniform(rng, 1.0, 2.0)
#     if size is None:
#         size = pf.random.uniform(rng, 0.01, 0.1)
#     if stretch is None:
#         stretch = pf.random.uniform(rng, 1.0, 4.0)
#     if rotation is None:
#         rotation = pf.random.uniform(
#             rng, np.array([0.0, 0.0, 0.0]), np.array([0.0, math.pi / 2, 0.0])
#         )
#     if w is None:
#         w = 0.0
#     if detail is None:
#         detail = 4.0
#     if spread is None:
#         spread = pf.random.uniform(rng, 0.2293, 0.5)
#     if smoothness is None:
#         smoothness = pf.random.uniform(rng, 0.596, 1.0)
#     if distortion_strength is None:
#         distortion_strength = pf.random.uniform(rng, 0.3, 1.2)
#     if invert is None:
#         invert = 0.0
#
#     sandstone_displacement_layer_result = sandstone_displacement_layer(
#         vector=vector,
#         base_height=base_height,
#         noise_type=noise_type,
#         height=height,
#         height_offset=height_offset,
#         mix_mode=mix_mode,
#         size=size,
#         stretch=stretch,
#         rotation=rotation,
#         w=w,
#         detail=detail,
#         spread=spread,
#         smoothness=smoothness,
#         distortion_strength=distortion_strength,
#         invert=invert,
#     )
#     return sandstone_displacement_layer_result


# def sandstone_color_layer_distribution(
#     rng: pf.RNG,
#     vector: t.SocketOrVal[pf.Vector],
#     base_color: t.SocketOrVal[pf.Color | None] = None,
#     noise_type: t.SocketOrVal[float | None] = None,
#     texture_color: t.SocketOrVal[pf.Color | None] = None,
#     mix_mode: t.SocketOrVal[float | None] = None,
#     size: t.SocketOrVal[float | None] = None,
#     stretch: t.SocketOrVal[float | None] = None,
#     rotation: t.SocketOrVal[pf.Vector | None] = None,
#     w: t.SocketOrVal[float | None] = None,
#     detail: t.SocketOrVal[float | None] = None,
#     spread: t.SocketOrVal[float | None] = None,
#     smoothness: t.SocketOrVal[float | None] = None,
#     distortion_strength: t.SocketOrVal[float | None] = None,
#     invert: t.SocketOrVal[float | None] = None,
#     mask: t.SocketOrVal[float | None] = None,
# ):
#     if base_color is None:
#         base_color_hsv = pf.random.uniform(
#             rng,
#             np.array([0.01129944, 0.137931, 0.029]),
#             np.array([0.12500006, 1.0, 0.236]),
#         )
#         base_color = pf.color.hsv_to_rgba(base_color_hsv)
#     else:
#         h_offset = pf.random.uniform(rng, -0.05, 0.05)
#         s_offset = pf.random.uniform(rng, -0.1, 0.1)
#         v_offset = pf.random.uniform(rng, -0.15, 0.15)
#         base_color = pf.nodes.shader.hue_saturation(
#             color=base_color,
#             hue=h_offset + 0.5,
#             saturation=s_offset + 1.0,
#             value=v_offset + 1.0,
#         )
#     if noise_type is None:
#         noise_type = 2.0
#     if texture_color is None:
#         texture_color_hsv = pf.random.uniform(
#             rng,
#             np.array([0.02983539, 0.03260868, 0.059]),
#             np.array([0.08333318, 0.97297299, 0.333]),
#         )
#         texture_color = pf.color.hsv_to_rgba(texture_color_hsv)
#     if mix_mode is None:
#         mix_mode = 1.0
#     if size is None:
#         size = pf.random.uniform(rng, 0.8, 1.5)
#     if stretch is None:
#         stretch = pf.random.uniform(rng, 0.7, 1.3)
#     if rotation is None:
#         rotation = (0.0, 0.0, 0.0)
#     if w is None:
#         w = pf.random.uniform(rng, -0.2, 0.2)
#     if detail is None:
#         detail = pf.random.uniform(rng, 6.0, 10.0)
#     if spread is None:
#         spread = pf.random.uniform(rng, 0.5453, 0.9173)
#     if smoothness is None:
#         smoothness = pf.random.uniform(rng, 0.352, 0.808)
#     if distortion_strength is None:
#         distortion_strength = pf.random.uniform(rng, 0.7, 1.3)
#     if invert is None:
#         invert = 0.0
#     if mask is None:
#         mask = 1.0
#
#     sandstone_color_layer_result = sandstone_color_layer(
#         vector=vector,
#         base_color=base_color,
#         noise_type=noise_type,
#         texture_color=texture_color,
#         mix_mode=mix_mode,
#         size=size,
#         stretch=stretch,
#         rotation=rotation,
#         w=w,
#         detail=detail,
#         spread=spread,
#         smoothness=smoothness,
#         distortion_strength=distortion_strength,
#         invert=invert,
#         mask=mask,
#     )
#     return sandstone_color_layer_result


def _stone_sandstone_distribution(  # noqa: C901
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color | None] = None,
    texture_color_layer1: t.SocketOrVal[pf.Color | None] = None,
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    texture_color_layer2: t.SocketOrVal[pf.Color | None] = None,
    w_layer2: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    mask_layer2: t.SocketOrVal[float | None] = None,
    texture_color_layer3: t.SocketOrVal[pf.Color | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    height_disp1: t.SocketOrVal[float | None] = None,
    size_disp1: t.SocketOrVal[float | None] = None,
    stretch_disp1: t.SocketOrVal[float | None] = None,
    rotation_disp1: t.SocketOrVal[pf.Vector | None] = None,
    spread_disp1: t.SocketOrVal[float | None] = None,
    smoothness_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    height_disp2: t.SocketOrVal[float | None] = None,
    size_disp2: t.SocketOrVal[float | None] = None,
    stretch_disp2: t.SocketOrVal[float | None] = None,
    rotation_disp2: t.SocketOrVal[pf.Vector | None] = None,
    spread_disp2: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
    height_disp3: t.SocketOrVal[float | None] = None,
    size_disp3: t.SocketOrVal[float | None] = None,
    rotation_disp3: t.SocketOrVal[pf.Vector | None] = None,
    spread_disp3: t.SocketOrVal[float | None] = None,
    smoothness_disp3: t.SocketOrVal[float | None] = None,
    distortion_strength_disp3: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if base_color is None:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.03, 0.35, 0.25]),
            np.array([0.14, 0.85, 0.58]),
        )
        base_color = pf.color.hsv_to_rgba(base_color_hsv)
    else:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.03, 0.35, 0.25]),
            np.array([0.14, 0.85, 0.58]),
        )

    if texture_color_layer1 is None:
        h_perturb = (pf.random.beta(rng, 0.4, 0.9) - 0.3) * 0.16
        s_perturb = pf.random.uniform(rng, -0.20, 0.20)
        v_perturb = pf.random.uniform(rng, -0.12, 0.18)
        texture_color_layer1_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer1 = pf.color.hsv_to_rgba(texture_color_layer1_hsv)
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.4, 1.1)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.35, 0.9)

    color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_layer1,
        mix_mode=1.0,
        size=1.0,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    if texture_color_layer2 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.5) - 0.5) * 0.20
        s_perturb = pf.random.uniform(rng, -0.20, 0.30)
        v_perturb = pf.random.uniform(rng, -0.15, 0.15)
        texture_color_layer2_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer2 = pf.color.hsv_to_rgba(texture_color_layer2_hsv)
    if w_layer2 is None:
        w_layer2 = pf.random.uniform(rng, 4.0, 12.0)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.4, 1.1)
    if mask_layer2 is None:
        mask_layer2 = pf.random.uniform(rng, 0.3, 0.7)

    color_layer_result_1 = sandstone_color_layer(
        vector=color_layer_result.vector,
        base_color=color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_layer2,
        mix_mode=1.0,
        size=0.5,
        stretch=1.0,
        w=w_layer2,
        detail=8.0,
        spread=spread_layer2,
        smoothness=pf.random.uniform(rng, 0.6, 0.95),
        mask=mask_layer2,
    )

    if texture_color_layer3 is None:
        h_perturb = (pf.random.beta(rng, 0.6, 0.6) - 0.5) * 0.12
        s_perturb = pf.random.uniform(rng, -0.225, 0.225)
        v_perturb = pf.random.uniform(rng, -0.15, 0.15)
        texture_color_layer3_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer3 = pf.color.hsv_to_rgba(texture_color_layer3_hsv)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.4, 1.1)

    mask_layer3 = pf.random.uniform(rng, 0.25, 0.65)
    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_layer3,
        mix_mode=2.0,
        size=0.05,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=pf.random.uniform(rng, 0.5, 0.9),
        mask=mask_layer3,
    )

    if roughness is None:
        roughness = pf.random.uniform(rng, 0.78, 0.95)
    if specular_ior_level is None:
        specular_ior_level = 0.05

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_2.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    if height_disp1 is None:
        height_disp1 = pf.random.uniform(rng, 0.2, 0.7)
    if size_disp1 is None:
        size_disp1 = pf.random.uniform(rng, 0.008, 0.06)
    if stretch_disp1 is None:
        stretch_disp1 = pf.random.uniform(rng, 3.0, 60.0)
    if rotation_disp1 is None:
        rotation_disp1 = (
            pf.random.uniform(rng, -0.7, 0.7),
            pf.random.uniform(rng, 1.3, 1.8708),
            pf.random.uniform(rng, -0.7, 0.7),
        )
    if spread_disp1 is None:
        spread_disp1 = pf.random.uniform(rng, 0.4, 1.1)
    if smoothness_disp1 is None:
        smoothness_disp1 = pf.random.uniform(rng, 0.5, 0.98)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 0.6, 2.0)

    displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        noise_type=2.0,
        height=height_disp1,
        mix_mode=1.0,
        size=size_disp1,
        stretch=stretch_disp1,
        rotation=rotation_disp1,
        detail=6.0,
        spread=spread_disp1,
        smoothness=smoothness_disp1,
        distortion_strength=distortion_strength_disp1,
    )

    if height_disp2 is None:
        height_disp2 = pf.random.uniform(rng, 0.15, 0.55)
    if size_disp2 is None:
        size_disp2 = pf.random.uniform(rng, 0.015, 0.08)
    if stretch_disp2 is None:
        stretch_disp2 = pf.random.uniform(rng, 4.0, 50.0)
    if rotation_disp2 is None:
        rotation_disp2 = (
            pf.random.uniform(rng, 1.3, 1.8708),
            pf.random.uniform(rng, -1.6708, -1.3),
            pf.random.uniform(rng, 1.3, 1.8708),
        )
    if spread_disp2 is None:
        spread_disp2 = pf.random.uniform(rng, 0.4, 1.1)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.7, 1.0)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 0.8, 2.0)

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=displacement_layer_result.vector,
        base_height=displacement_layer_result.height,
        noise_type=2.0,
        height=height_disp2,
        mix_mode=1.0,
        size=size_disp2,
        stretch=stretch_disp2,
        rotation=rotation_disp2,
        detail=8.0,
        spread=spread_disp2,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )

    if height_disp3 is None:
        height_disp3 = pf.random.uniform(rng, 0.03, 0.15)
    if size_disp3 is None:
        size_disp3 = pf.random.uniform(rng, 0.0008, 0.007)
    if rotation_disp3 is None:
        rotation_disp3 = (
            pf.random.uniform(rng, -0.5, 0.5),
            pf.random.uniform(rng, -0.5, 0.5),
            pf.random.uniform(rng, -0.5, 0.5),
        )
    if spread_disp3 is None:
        spread_disp3 = pf.random.uniform(rng, 0.4, 1.1)
    if smoothness_disp3 is None:
        smoothness_disp3 = pf.random.uniform(rng, 0.6, 0.95)
    if distortion_strength_disp3 is None:
        distortion_strength_disp3 = pf.random.uniform(rng, 0.4, 1.3)

    displacement_layer_result_2 = sandstone_displacement_layer(
        vector=displacement_layer_result_1.vector,
        base_height=displacement_layer_result_1.height,
        noise_type=2.0,
        height=height_disp3,
        mix_mode=1.0,
        size=size_disp3,
        stretch=1.0,
        rotation=rotation_disp3,
        detail=6.0,
        spread=spread_disp3,
        smoothness=smoothness_disp3,
        distortion_strength=distortion_strength_disp3,
    )

    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_2.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )

    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def _stone_limestone_distribution(  # noqa: C901
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color | None] = None,
    texture_color_layer1: t.SocketOrVal[pf.Color | None] = None,
    size_layer1: t.SocketOrVal[float | None] = None,
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    texture_color_layer2: t.SocketOrVal[pf.Color | None] = None,
    size_layer2: t.SocketOrVal[float | None] = None,
    w_layer2: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    mask_layer2: t.SocketOrVal[float | None] = None,
    texture_color_layer3: t.SocketOrVal[pf.Color | None] = None,
    size_layer3: t.SocketOrVal[float | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    mask_layer3: t.SocketOrVal[float | None] = None,
    texture_color_layer4: t.SocketOrVal[pf.Color | None] = None,
    size_layer4: t.SocketOrVal[float | None] = None,
    spread_layer4: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    height_disp1: t.SocketOrVal[float | None] = None,
    size_disp1: t.SocketOrVal[float | None] = None,
    stretch_disp1: t.SocketOrVal[float | None] = None,
    spread_disp1: t.SocketOrVal[float | None] = None,
    smoothness_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    height_disp2: t.SocketOrVal[float | None] = None,
    size_disp2: t.SocketOrVal[float | None] = None,
    stretch_disp2: t.SocketOrVal[float | None] = None,
    spread_disp2: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
    height_disp3: t.SocketOrVal[float | None] = None,
    size_disp3: t.SocketOrVal[float | None] = None,
    spread_disp3: t.SocketOrVal[float | None] = None,
    smoothness_disp3: t.SocketOrVal[float | None] = None,
    distortion_strength_disp3: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if base_color is None:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.07, 0.15, 0.52]),
            np.array([0.12, 0.40, 0.72]),
        )
        base_color = pf.color.hsv_to_rgba(base_color_hsv)
    else:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.07, 0.15, 0.52]),
            np.array([0.12, 0.40, 0.72]),
        )

    if texture_color_layer1 is None:
        h_perturb = (pf.random.beta(rng, 0.4, 0.8) - 0.35) * 0.12
        s_perturb = pf.random.uniform(rng, -0.125, 0.125)
        v_perturb = pf.random.uniform(rng, -0.112, 0.168)
        texture_color_layer1_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer1 = pf.color.hsv_to_rgba(texture_color_layer1_hsv)
    if size_layer1 is None:
        size_layer1 = pf.random.uniform(rng, 0.5, 2.0)
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.65, 1.0)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.5, 0.95)

    color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_layer1,
        mix_mode=1.0,
        size=size_layer1,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    if texture_color_layer2 is None:
        h_perturb = (pf.random.beta(rng, 0.6, 0.5) - 0.5) * 0.14
        s_perturb = pf.random.uniform(rng, -0.112, 0.168)
        v_perturb = pf.random.uniform(rng, -0.15, 0.15)
        texture_color_layer2_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer2 = pf.color.hsv_to_rgba(texture_color_layer2_hsv)
    if size_layer2 is None:
        size_layer2 = pf.random.uniform(rng, 0.5, 2.0)
    if w_layer2 is None:
        w_layer2 = pf.random.uniform(rng, 0.0, 20.0)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.65, 1.0)
    if mask_layer2 is None:
        mask_layer2 = pf.random.uniform(rng, 0.3, 0.5)

    color_layer_result_1 = sandstone_color_layer(
        vector=color_layer_result.vector,
        base_color=color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_layer2,
        mix_mode=1.0,
        size=size_layer2,
        stretch=1.0,
        w=w_layer2,
        detail=8.0,
        spread=spread_layer2,
        smoothness=1.0,
        mask=mask_layer2,
    )

    if texture_color_layer3 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.4) - 0.5) * 0.12
        s_perturb = pf.random.uniform(rng, -0.11, 0.11)
        v_perturb = pf.random.uniform(rng, -0.176, 0.144)
        texture_color_layer3_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer3 = pf.color.hsv_to_rgba(texture_color_layer3_hsv)
    if size_layer3 is None:
        size_layer3 = pf.random.uniform(rng, 0.03, 0.08)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.65, 1.0)
    if mask_layer3 is None:
        mask_layer3 = pf.random.uniform(rng, 0.10, 0.30)

    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_layer3,
        mix_mode=1.0,
        size=size_layer3,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=1.0,
        mask=mask_layer3,
    )

    if texture_color_layer4 is None:
        h_perturb = (pf.random.beta(rng, 0.4, 0.5) - 0.45) * 0.11
        s_perturb = pf.random.uniform(rng, -0.12, 0.12)
        v_perturb = pf.random.uniform(rng, -0.104, 0.156)
        texture_color_layer4_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer4 = pf.color.hsv_to_rgba(texture_color_layer4_hsv)
    if size_layer4 is None:
        size_layer4 = pf.random.uniform(rng, 0.5, 2.0)
    if spread_layer4 is None:
        spread_layer4 = pf.random.uniform(rng, 0.65, 1.0)

    color_layer_result_3 = sandstone_color_layer(
        vector=color_layer_result_2.vector,
        base_color=color_layer_result_2.color,
        texture_color=texture_color_layer4,
        mix_mode=2.0,
        size=size_layer4,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer4,
        smoothness=1.0,
        mask=0.25,
    )

    if roughness is None:
        roughness = pf.random.uniform(rng, 0.60, 0.80)
    if specular_ior_level is None:
        specular_ior_level = 0.04

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_3.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    if height_disp1 is None:
        height_disp1 = pf.random.uniform(rng, 0.25, 0.45)
    if size_disp1 is None:
        size_disp1 = pf.random.uniform(rng, 0.05, 0.15)
    if stretch_disp1 is None:
        stretch_disp1 = pf.random.uniform(rng, 1.0, 2.0)
    if spread_disp1 is None:
        spread_disp1 = pf.random.uniform(rng, 0.80, 1.0)
    if smoothness_disp1 is None:
        smoothness_disp1 = pf.random.uniform(rng, 0.80, 1.0)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 0.4, 0.8)

    displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        noise_type=2.0,
        height=height_disp1,
        mix_mode=1.0,
        size=size_disp1,
        stretch=stretch_disp1,
        rotation=(0.0, 0.0, 0.0),
        detail=6.0,
        spread=spread_disp1,
        smoothness=smoothness_disp1,
        distortion_strength=distortion_strength_disp1,
    )

    if height_disp2 is None:
        height_disp2 = pf.random.uniform(rng, 0.12, 0.28)
    if size_disp2 is None:
        size_disp2 = pf.random.uniform(rng, 0.08, 0.2)
    if stretch_disp2 is None:
        stretch_disp2 = pf.random.uniform(rng, 1.0, 1.8)
    if spread_disp2 is None:
        spread_disp2 = pf.random.uniform(rng, 0.80, 1.0)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.85, 1.0)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 0.2, 0.5)

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=displacement_layer_result.vector,
        base_height=displacement_layer_result.height,
        noise_type=2.0,
        height=height_disp2,
        mix_mode=1.0,
        size=size_disp2,
        stretch=stretch_disp2,
        rotation=(0.0, 0.0, 0.0),
        detail=8.0,
        spread=spread_disp2,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )

    if height_disp3 is None:
        height_disp3 = pf.random.uniform(rng, 0.04, 0.10)
    if size_disp3 is None:
        size_disp3 = pf.random.uniform(rng, 0.003, 0.015)
    if spread_disp3 is None:
        spread_disp3 = pf.random.uniform(rng, 0.80, 1.0)
    if smoothness_disp3 is None:
        smoothness_disp3 = pf.random.uniform(rng, 0.85, 1.0)
    if distortion_strength_disp3 is None:
        distortion_strength_disp3 = pf.random.uniform(rng, 0.15, 0.35)

    displacement_layer_result_2 = sandstone_displacement_layer(
        vector=displacement_layer_result_1.vector,
        base_height=displacement_layer_result_1.height,
        noise_type=2.0,
        height=height_disp3,
        mix_mode=1.0,
        size=size_disp3,
        stretch=1.0,
        rotation=(0.0, 0.0, 0.0),
        detail=6.0,
        spread=spread_disp3,
        smoothness=smoothness_disp3,
        distortion_strength=distortion_strength_disp3,
    )

    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_2.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )

    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def _stone_basalt_distribution(  # noqa: C901
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color | None] = None,
    texture_color_layer1: t.SocketOrVal[pf.Color | None] = None,
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    texture_color_layer2: t.SocketOrVal[pf.Color | None] = None,
    w_layer2: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    mask_layer2: t.SocketOrVal[float | None] = None,
    texture_color_layer3: t.SocketOrVal[pf.Color | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    height_disp1: t.SocketOrVal[float | None] = None,
    size_disp1: t.SocketOrVal[float | None] = None,
    stretch_disp1: t.SocketOrVal[float | None] = None,
    spread_disp1: t.SocketOrVal[float | None] = None,
    smoothness_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    height_disp2: t.SocketOrVal[float | None] = None,
    size_disp2: t.SocketOrVal[float | None] = None,
    stretch_disp2: t.SocketOrVal[float | None] = None,
    spread_disp2: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if base_color is None:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.0, 0.0, 0.015]),
            np.array([0.03, 0.06, 0.045]),
        )
        base_color = pf.color.hsv_to_rgba(base_color_hsv)
    else:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.0, 0.0, 0.015]),
            np.array([0.03, 0.06, 0.045]),
        )

    if texture_color_layer1 is None:
        h_perturb = (pf.random.beta(rng, 0.4, 0.7) - 0.4) * 0.06
        s_perturb = pf.random.uniform(rng, -0.04, 0.04)
        v_perturb = pf.random.uniform(rng, -0.016, 0.016)
        texture_color_layer1_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer1 = pf.color.hsv_to_rgba(texture_color_layer1_hsv)
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.35, 0.90)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.3, 0.85)

    color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_layer1,
        mix_mode=1.0,
        size=1.0,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    if texture_color_layer2 is None:
        h_perturb = (pf.random.beta(rng, 0.6, 0.6) - 0.5) * 0.06
        s_perturb = pf.random.uniform(rng, -0.054, 0.066)
        v_perturb = pf.random.uniform(rng, -0.0175, 0.0175)
        texture_color_layer2_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer2 = pf.color.hsv_to_rgba(texture_color_layer2_hsv)
    if w_layer2 is None:
        w_layer2 = pf.random.uniform(rng, 2.0, 5.0)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.6, 0.95)
    if mask_layer2 is None:
        mask_layer2 = pf.random.uniform(rng, 0.30, 0.60)

    color_layer_result_1 = sandstone_color_layer(
        vector=color_layer_result.vector,
        base_color=color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_layer2,
        mix_mode=1.0,
        size=0.5,
        stretch=1.0,
        w=w_layer2,
        detail=8.0,
        spread=spread_layer2,
        smoothness=1.0,
        mask=mask_layer2,
    )

    if texture_color_layer3 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.5) - 0.5) * 0.06
        s_perturb = pf.random.uniform(rng, -0.06, 0.06)
        v_perturb = pf.random.uniform(rng, -0.0175, 0.0175)
        texture_color_layer3_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer3 = pf.color.hsv_to_rgba(texture_color_layer3_hsv)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.35, 0.85)

    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_layer3,
        mix_mode=2.0,
        size=0.08,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=1.0,
        mask=0.3,
    )

    if roughness is None:
        roughness = pf.random.uniform(rng, 0.80, 0.98)
    if specular_ior_level is None:
        specular_ior_level = pf.random.uniform(rng, 0.01, 0.04)

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_2.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    if height_disp1 is None:
        height_disp1 = pf.random.uniform(rng, 0.07, 0.16)
    if size_disp1 is None:
        size_disp1 = pf.random.uniform(rng, 0.02, 0.06)
    if stretch_disp1 is None:
        stretch_disp1 = pf.random.uniform(rng, 1.0, 1.3)
    if spread_disp1 is None:
        spread_disp1 = pf.random.uniform(rng, 0.55, 0.9)
    if smoothness_disp1 is None:
        smoothness_disp1 = pf.random.uniform(rng, 0.4, 0.85)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 0.15, 0.50)

    displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        noise_type=2.0,
        height=height_disp1,
        mix_mode=1.0,
        size=size_disp1,
        stretch=stretch_disp1,
        rotation=(0.0, 0.0, 0.0),
        detail=8.0,
        spread=spread_disp1,
        smoothness=smoothness_disp1,
        distortion_strength=distortion_strength_disp1,
    )

    if height_disp2 is None:
        height_disp2 = pf.random.uniform(rng, 0.04, 0.10)
    if size_disp2 is None:
        size_disp2 = pf.random.uniform(rng, 0.03, 0.07)
    if stretch_disp2 is None:
        stretch_disp2 = pf.random.uniform(rng, 1.0, 1.3)
    if spread_disp2 is None:
        spread_disp2 = pf.random.uniform(rng, 0.55, 0.9)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.5, 0.9)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 0.12, 0.45)

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=displacement_layer_result.vector,
        base_height=displacement_layer_result.height,
        noise_type=2.0,
        height=height_disp2,
        mix_mode=1.0,
        size=size_disp2,
        stretch=stretch_disp2,
        rotation=(0.0, 0.0, 0.0),
        detail=8.0,
        spread=spread_disp2,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )

    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_1.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )

    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def _stone_volcanic_distribution(  # noqa: C901
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color | None] = None,
    texture_color_layer1: t.SocketOrVal[pf.Color | None] = None,
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    texture_color_layer2: t.SocketOrVal[pf.Color | None] = None,
    w_layer2: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    smoothness_layer2: t.SocketOrVal[float | None] = None,
    mask_layer2: t.SocketOrVal[float | None] = None,
    texture_color_layer3: t.SocketOrVal[pf.Color | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    smoothness_layer3: t.SocketOrVal[float | None] = None,
    texture_color_layer4: t.SocketOrVal[pf.Color | None] = None,
    size_layer4: t.SocketOrVal[float | None] = None,
    spread_layer4: t.SocketOrVal[float | None] = None,
    smoothness_layer4: t.SocketOrVal[float | None] = None,
    mask_layer4: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    height_disp1: t.SocketOrVal[float | None] = None,
    size_disp1: t.SocketOrVal[float | None] = None,
    stretch_disp1: t.SocketOrVal[float | None] = None,
    rotation_disp1: t.SocketOrVal[pf.Vector | None] = None,
    spread_disp1: t.SocketOrVal[float | None] = None,
    smoothness_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    height_disp2: t.SocketOrVal[float | None] = None,
    size_disp2: t.SocketOrVal[float | None] = None,
    stretch_disp2: t.SocketOrVal[float | None] = None,
    rotation_disp2: t.SocketOrVal[pf.Vector | None] = None,
    spread_disp2: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
    height_disp3: t.SocketOrVal[float | None] = None,
    size_disp3: t.SocketOrVal[float | None] = None,
    stretch_disp3: t.SocketOrVal[float | None] = None,
    spread_disp3: t.SocketOrVal[float | None] = None,
    smoothness_disp3: t.SocketOrVal[float | None] = None,
    distortion_strength_disp3: t.SocketOrVal[float | None] = None,
    height_disp4: t.SocketOrVal[float | None] = None,
    size_disp4: t.SocketOrVal[float | None] = None,
    spread_disp4: t.SocketOrVal[float | None] = None,
    smoothness_disp4: t.SocketOrVal[float | None] = None,
    distortion_strength_disp4: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if base_color is None:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.0, 0.08, 0.005]),
            np.array([0.06, 0.28, 0.015]),
        )
        base_color = pf.color.hsv_to_rgba(base_color_hsv)
    else:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.0, 0.08, 0.005]),
            np.array([0.06, 0.28, 0.015]),
        )

    if texture_color_layer1 is None:
        h_perturb = (pf.random.beta(rng, 0.3, 0.8) - 0.35) * 0.08
        s_perturb = pf.random.uniform(rng, -0.14, 0.14)
        v_perturb = pf.random.uniform(rng, -0.0175, 0.0175)
        texture_color_layer1_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer1 = pf.color.hsv_to_rgba(texture_color_layer1_hsv)
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.25, 0.55)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.2, 0.5)

    color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_layer1,
        mix_mode=1.0,
        size=1.0,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    if texture_color_layer2 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.6) - 0.45) * 0.08
        s_perturb = pf.random.uniform(rng, -0.088, 0.132)
        v_perturb = pf.random.uniform(rng, -0.016, 0.016)
        texture_color_layer2_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer2 = pf.color.hsv_to_rgba(texture_color_layer2_hsv)
    if w_layer2 is None:
        w_layer2 = pf.random.uniform(rng, 4.0, 8.0)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.25, 0.55)
    if smoothness_layer2 is None:
        smoothness_layer2 = pf.random.uniform(rng, 0.25, 0.55)
    if mask_layer2 is None:
        mask_layer2 = pf.random.uniform(rng, 0.35, 0.6)

    color_layer_result_1 = sandstone_color_layer(
        vector=color_layer_result.vector,
        base_color=color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_layer2,
        mix_mode=1.0,
        size=0.5,
        stretch=1.0,
        w=w_layer2,
        detail=8.0,
        spread=spread_layer2,
        smoothness=smoothness_layer2,
        mask=mask_layer2,
    )

    if texture_color_layer3 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.7) - 0.5) * 0.06
        s_perturb = pf.random.uniform(rng, -0.12, 0.12)
        v_perturb = pf.random.uniform(rng, -0.0121, 0.0099)
        texture_color_layer3_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer3 = pf.color.hsv_to_rgba(texture_color_layer3_hsv)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.2, 0.5)
    if smoothness_layer3 is None:
        smoothness_layer3 = pf.random.uniform(rng, 0.2, 0.5)

    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_layer3,
        mix_mode=2.0,
        size=0.06,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=smoothness_layer3,
        mask=0.35,
    )

    if texture_color_layer4 is None:
        texture_color_layer4 = pf.color.hsv_to_rgba(
            pf.random.uniform(
                rng,
                np.array([0.0, 0.0, 0.004]),
                np.array([0.03, 0.15, 0.018]),
            )
        )
    if size_layer4 is None:
        size_layer4 = pf.random.uniform(rng, 0.008, 0.025)
    if spread_layer4 is None:
        spread_layer4 = pf.random.uniform(rng, 0.2, 0.5)
    if smoothness_layer4 is None:
        smoothness_layer4 = pf.random.uniform(rng, 0.2, 0.5)
    if mask_layer4 is None:
        mask_layer4 = pf.random.uniform(rng, 0.08, 0.18)

    color_layer_result_3 = sandstone_color_layer(
        vector=color_layer_result_2.vector,
        base_color=color_layer_result_2.color,
        texture_color=texture_color_layer4,
        mix_mode=1.0,
        size=size_layer4,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer4,
        smoothness=smoothness_layer4,
        mask=mask_layer4,
    )

    if roughness is None:
        roughness = pf.random.uniform(rng, 0.88, 0.98)
    if specular_ior_level is None:
        specular_ior_level = 0.02

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_3.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    if height_disp1 is None:
        height_disp1 = pf.random.uniform(rng, 0.50, 0.75)
    if size_disp1 is None:
        size_disp1 = pf.random.uniform(rng, 0.04, 0.12)
    if stretch_disp1 is None:
        stretch_disp1 = pf.random.uniform(rng, 1.5, 6.0)
    if rotation_disp1 is None:
        rotation_disp1 = (0.0, pf.random.uniform(rng, 0.0, math.pi / 2), 0.0)
    if spread_disp1 is None:
        spread_disp1 = pf.random.uniform(rng, 0.2, 0.5)
    if smoothness_disp1 is None:
        smoothness_disp1 = pf.random.uniform(rng, 0.2, 0.5)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 2.5, 4.0)

    displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        noise_type=2.0,
        height=height_disp1,
        mix_mode=1.0,
        size=size_disp1,
        stretch=stretch_disp1,
        rotation=rotation_disp1,
        detail=6.0,
        spread=spread_disp1,
        smoothness=smoothness_disp1,
        distortion_strength=distortion_strength_disp1,
    )

    if height_disp2 is None:
        height_disp2 = pf.random.uniform(rng, 0.35, 0.65)
    if size_disp2 is None:
        size_disp2 = pf.random.uniform(rng, 0.05, 0.14)
    if stretch_disp2 is None:
        stretch_disp2 = pf.random.uniform(rng, 1.0, 4.0)
    if rotation_disp2 is None:
        rotation_disp2 = (0.0, pf.random.uniform(rng, 0.0, math.pi / 2), 0.0)
    if spread_disp2 is None:
        spread_disp2 = pf.random.uniform(rng, 0.2, 0.5)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.25, 0.55)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 1.4, 2.8)

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=displacement_layer_result.vector,
        base_height=displacement_layer_result.height,
        noise_type=2.0,
        height=height_disp2,
        mix_mode=1.0,
        size=size_disp2,
        stretch=stretch_disp2,
        rotation=rotation_disp2,
        detail=8.0,
        spread=spread_disp2,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )

    if height_disp3 is None:
        height_disp3 = pf.random.uniform(rng, 0.30, 0.55)
    if size_disp3 is None:
        size_disp3 = pf.random.uniform(rng, 0.06, 0.16)
    if stretch_disp3 is None:
        stretch_disp3 = pf.random.uniform(rng, 1.0, 3.0)
    if spread_disp3 is None:
        spread_disp3 = pf.random.uniform(rng, 0.2, 0.5)
    if smoothness_disp3 is None:
        smoothness_disp3 = pf.random.uniform(rng, 0.3, 0.6)
    if distortion_strength_disp3 is None:
        distortion_strength_disp3 = pf.random.uniform(rng, 1.2, 2.5)

    displacement_layer_result_2 = sandstone_displacement_layer(
        vector=displacement_layer_result_1.vector,
        base_height=displacement_layer_result_1.height,
        noise_type=2.0,
        height=height_disp3,
        mix_mode=2.0,
        size=size_disp3,
        stretch=stretch_disp3,
        detail=6.0,
        spread=spread_disp3,
        smoothness=smoothness_disp3,
        distortion_strength=distortion_strength_disp3,
    )

    if height_disp4 is None:
        height_disp4 = pf.random.uniform(rng, 0.15, 0.30)
    if size_disp4 is None:
        size_disp4 = pf.random.uniform(rng, 0.004, 0.015)
    if spread_disp4 is None:
        spread_disp4 = pf.random.uniform(rng, 0.2, 0.5)
    if smoothness_disp4 is None:
        smoothness_disp4 = pf.random.uniform(rng, 0.3, 0.6)
    if distortion_strength_disp4 is None:
        distortion_strength_disp4 = pf.random.uniform(rng, 0.8, 1.8)

    displacement_layer_result_3 = sandstone_displacement_layer(
        vector=displacement_layer_result_2.vector,
        base_height=displacement_layer_result_2.height,
        noise_type=2.0,
        height=height_disp4,
        mix_mode=1.0,
        size=size_disp4,
        stretch=1.0,
        detail=6.0,
        spread=spread_disp4,
        smoothness=smoothness_disp4,
        distortion_strength=distortion_strength_disp4,
    )

    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_3.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )

    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def _stone_tuff_distribution(  # noqa: C901
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color | None] = None,
    texture_color_layer1: t.SocketOrVal[pf.Color | None] = None,
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    texture_color_layer2: t.SocketOrVal[pf.Color | None] = None,
    w_layer2: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    texture_color_layer3: t.SocketOrVal[pf.Color | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    height_disp1: t.SocketOrVal[float | None] = None,
    size_disp1: t.SocketOrVal[float | None] = None,
    spread_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    height_disp2: t.SocketOrVal[float | None] = None,
    size_disp2: t.SocketOrVal[float | None] = None,
    spread_disp2: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
    height_disp3: t.SocketOrVal[float | None] = None,
    size_disp3: t.SocketOrVal[float | None] = None,
    detail_disp3: t.SocketOrVal[float | None] = None,
    spread_disp3: t.SocketOrVal[float | None] = None,
    distortion_strength_disp3: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if base_color is None:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.04, 0.08, 0.38]),
            np.array([0.16, 0.30, 0.62]),
        )
        base_color = pf.color.hsv_to_rgba(base_color_hsv)
    else:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.04, 0.08, 0.38]),
            np.array([0.16, 0.30, 0.62]),
        )

    if texture_color_layer1 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.6) - 0.45) * 0.16
        s_perturb = pf.random.uniform(rng, -0.13, 0.13)
        v_perturb = pf.random.uniform(rng, -0.14, 0.14)
        texture_color_layer1_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer1 = pf.color.hsv_to_rgba(texture_color_layer1_hsv)
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.3, 0.7)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.3, 0.5)

    color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_layer1,
        mix_mode=1.0,
        size=1.0,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    if texture_color_layer2 is None:
        h_perturb = (pf.random.beta(rng, 0.4, 0.4) - 0.3) * 0.32
        s_perturb = pf.random.uniform(rng, -0.18, 0.22)
        v_perturb = pf.random.uniform(rng, -0.13, 0.13)
        texture_color_layer2_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer2 = pf.color.hsv_to_rgba(texture_color_layer2_hsv)
    if w_layer2 is None:
        w_layer2 = pf.random.uniform(rng, 1.0, 4.5)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.40, 0.65)

    color_layer_result_1 = sandstone_color_layer(
        vector=color_layer_result.vector,
        base_color=color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_layer2,
        mix_mode=2.0,
        size=1.0,
        stretch=1.0,
        w=w_layer2,
        detail=8.0,
        spread=spread_layer2,
        smoothness=1.0,
    )

    if texture_color_layer3 is None:
        h_perturb = (pf.random.beta(rng, 0.7, 0.5) - 0.55) * 0.16
        s_perturb = pf.random.uniform(rng, -0.11, 0.09)
        v_perturb = pf.random.uniform(rng, -0.176, 0.144)
        texture_color_layer3_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer3 = pf.color.hsv_to_rgba(texture_color_layer3_hsv)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.3, 0.5)

    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_layer3,
        mix_mode=2.0,
        size=0.05,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=1.0,
        mask=0.3,
    )

    if roughness is None:
        roughness = pf.random.uniform(rng, 0.82, 0.92)
    if specular_ior_level is None:
        specular_ior_level = 0.05

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_2.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    if height_disp1 is None:
        height_disp1 = pf.random.uniform(rng, 0.5, 0.8)
    if size_disp1 is None:
        size_disp1 = pf.random.uniform(rng, 0.08, 0.15)
    if spread_disp1 is None:
        spread_disp1 = pf.random.uniform(rng, 0.2, 0.35)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 0.25, 0.4)

    displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        height=height_disp1,
        mix_mode=2.0,
        size=size_disp1,
        stretch=1.0,
        detail=4.0,
        spread=spread_disp1,
        smoothness=1.0,
        distortion_strength=distortion_strength_disp1,
    )

    if height_disp2 is None:
        height_disp2 = pf.random.uniform(rng, 0.4, 0.6)
    if size_disp2 is None:
        size_disp2 = pf.random.uniform(rng, 0.04, 0.08)
    if spread_disp2 is None:
        spread_disp2 = pf.random.uniform(rng, 0.8, 1.0)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.9, 1.0)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 0.25, 0.4)

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=displacement_layer_result.vector,
        base_height=displacement_layer_result.height,
        noise_type=2.0,
        height=height_disp2,
        mix_mode=1.0,
        size=size_disp2,
        stretch=1.0,
        detail=8.0,
        spread=spread_disp2,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )

    if height_disp3 is None:
        height_disp3 = pf.random.uniform(rng, 0.4, 0.6)
    if size_disp3 is None:
        size_disp3 = pf.random.uniform(rng, 0.08, 0.12)
    if detail_disp3 is None:
        detail_disp3 = pf.random.uniform(rng, 5.0, 6.0)
    if spread_disp3 is None:
        spread_disp3 = pf.random.uniform(rng, 0.65, 0.75)
    if distortion_strength_disp3 is None:
        distortion_strength_disp3 = pf.random.uniform(rng, 0.4, 0.6)

    displacement_layer_result_2 = sandstone_displacement_layer(
        vector=displacement_layer_result_1.vector,
        base_height=displacement_layer_result_1.height,
        noise_type=2.0,
        height=height_disp3,
        mix_mode=2.0,
        size=size_disp3,
        stretch=1.0,
        detail=detail_disp3,
        spread=spread_disp3,
        smoothness=1.0,
        distortion_strength=distortion_strength_disp3,
    )

    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_2.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )

    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def _stone_grey_distribution(  # noqa: C901
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color | None] = None,
    texture_color_layer1: t.SocketOrVal[pf.Color | None] = None,
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    texture_color_layer2: t.SocketOrVal[pf.Color | None] = None,
    w_layer2: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    texture_color_layer3: t.SocketOrVal[pf.Color | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    height_disp1: t.SocketOrVal[float | None] = None,
    size_disp1: t.SocketOrVal[float | None] = None,
    spread_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    height_disp2: t.SocketOrVal[float | None] = None,
    size_disp2: t.SocketOrVal[float | None] = None,
    stretch_disp2: t.SocketOrVal[float | None] = None,
    rotation_disp2: t.SocketOrVal[pf.Vector | None] = None,
    spread_disp2: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if base_color is None:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.0, 0.0, 0.10]),
            np.array([0.03, 0.04, 0.30]),
        )
        base_color = pf.color.hsv_to_rgba(base_color_hsv)
    else:
        base_color_hsv = pf.random.uniform(
            rng,
            np.array([0.0, 0.0, 0.10]),
            np.array([0.03, 0.04, 0.30]),
        )

    if texture_color_layer1 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.6) - 0.45) * 0.08
        s_perturb = pf.random.uniform(rng, -0.04, 0.04)
        v_perturb = pf.random.uniform(rng, -0.088, 0.132)
        texture_color_layer1_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer1 = pf.color.hsv_to_rgba(texture_color_layer1_hsv)
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.40, 0.90)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.40, 0.85)

    color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_layer1,
        mix_mode=1.0,
        size=1.0,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    if texture_color_layer2 is None:
        h_perturb = (pf.random.beta(rng, 0.6, 0.6) - 0.5) * 0.10
        s_perturb = pf.random.uniform(rng, -0.054, 0.066)
        v_perturb = pf.random.uniform(rng, -0.07, 0.07)
        texture_color_layer2_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer2 = pf.color.hsv_to_rgba(texture_color_layer2_hsv)
    if w_layer2 is None:
        w_layer2 = pf.random.uniform(rng, 1.0, 2.5)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.30, 0.50)

    color_layer_result_1 = sandstone_color_layer(
        vector=color_layer_result.vector,
        base_color=color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_layer2,
        mix_mode=2.0,
        size=1.0,
        stretch=1.0,
        w=w_layer2,
        detail=8.0,
        spread=spread_layer2,
        smoothness=1.0,
        mask=0.55,
    )

    if texture_color_layer3 is None:
        h_perturb = (pf.random.beta(rng, 0.5, 0.5) - 0.5) * 0.10
        s_perturb = pf.random.uniform(rng, -0.04, 0.04)
        v_perturb = pf.random.uniform(rng, -0.09, 0.09)
        texture_color_layer3_hsv = base_color_hsv + np.array(
            [h_perturb, s_perturb, v_perturb]
        )
        texture_color_layer3 = pf.color.hsv_to_rgba(texture_color_layer3_hsv)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.25, 0.42)

    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_layer3,
        mix_mode=2.0,
        size=0.05,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=1.0,
        mask=0.5,
    )

    if roughness is None:
        roughness = pf.random.uniform(rng, 0.70, 0.92)
    if specular_ior_level is None:
        specular_ior_level = 0.05

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_2.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    if height_disp1 is None:
        height_disp1 = pf.random.uniform(rng, 0.18, 0.48)
    if size_disp1 is None:
        size_disp1 = pf.random.uniform(rng, 0.08, 0.14)
    if spread_disp1 is None:
        spread_disp1 = pf.random.uniform(rng, 0.25, 0.40)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 0.28, 0.45)

    displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        height=height_disp1,
        mix_mode=2.0,
        size=size_disp1,
        stretch=1.0,
        detail=4.0,
        spread=spread_disp1,
        smoothness=1.0,
        distortion_strength=distortion_strength_disp1,
    )

    if height_disp2 is None:
        height_disp2 = pf.random.uniform(rng, 0.28, 0.78)
    if size_disp2 is None:
        size_disp2 = pf.random.uniform(rng, 0.06, 0.1)
    if stretch_disp2 is None:
        stretch_disp2 = pf.random.uniform(rng, 1.4, 1.6)
    if rotation_disp2 is None:
        rotation_disp2 = (0.0, pf.random.uniform(rng, 1.4708, 1.6708), 0.0)
    if spread_disp2 is None:
        spread_disp2 = pf.random.uniform(rng, 0.9, 1.0)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.9, 1.0)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 0.25, 0.35)

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=displacement_layer_result.vector,
        base_height=displacement_layer_result.height,
        noise_type=2.0,
        height=height_disp2,
        mix_mode=1.0,
        size=size_disp2,
        stretch=stretch_disp2,
        rotation=rotation_disp2,
        detail=8.0,
        spread=spread_disp2,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )

    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_1.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )

    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def stone_smooth_distribution(  # noqa: C901
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
    base_color: t.SocketOrVal[pf.Color | None] = None,
    texture_color_layer1: t.SocketOrVal[pf.Color | None] = None,
    size_layer1: t.SocketOrVal[float | None] = None,
    spread_layer1: t.SocketOrVal[float | None] = None,
    smoothness_layer1: t.SocketOrVal[float | None] = None,
    texture_color_layer2: t.SocketOrVal[pf.Color | None] = None,
    size_layer2: t.SocketOrVal[float | None] = None,
    w_layer2: t.SocketOrVal[float | None] = None,
    spread_layer2: t.SocketOrVal[float | None] = None,
    mask_layer2: t.SocketOrVal[float | None] = None,
    texture_color_layer3: t.SocketOrVal[pf.Color | None] = None,
    size_layer3: t.SocketOrVal[float | None] = None,
    spread_layer3: t.SocketOrVal[float | None] = None,
    mask_layer3: t.SocketOrVal[float | None] = None,
    texture_color_layer4: t.SocketOrVal[pf.Color | None] = None,
    size_layer4: t.SocketOrVal[float | None] = None,
    spread_layer4: t.SocketOrVal[float | None] = None,
    roughness: t.SocketOrVal[float | None] = None,
    specular_ior_level: t.SocketOrVal[float | None] = None,
    height_disp1: t.SocketOrVal[float | None] = None,
    size_disp1: t.SocketOrVal[float | None] = None,
    stretch_disp1: t.SocketOrVal[float | None] = None,
    spread_disp1: t.SocketOrVal[float | None] = None,
    smoothness_disp1: t.SocketOrVal[float | None] = None,
    distortion_strength_disp1: t.SocketOrVal[float | None] = None,
    height_disp2: t.SocketOrVal[float | None] = None,
    size_disp2: t.SocketOrVal[float | None] = None,
    stretch_disp2: t.SocketOrVal[float | None] = None,
    spread_disp2: t.SocketOrVal[float | None] = None,
    smoothness_disp2: t.SocketOrVal[float | None] = None,
    distortion_strength_disp2: t.SocketOrVal[float | None] = None,
    height_disp3: t.SocketOrVal[float | None] = None,
    size_disp3: t.SocketOrVal[float | None] = None,
    spread_disp3: t.SocketOrVal[float | None] = None,
    smoothness_disp3: t.SocketOrVal[float | None] = None,
    distortion_strength_disp3: t.SocketOrVal[float | None] = None,
) -> pf.Material:
    if base_color is None:
        h_base = pf.random.clip_gaussian(rng, 0.15, 0.1, 0.05, 0.25)
        s_base = pf.random.uniform(rng, 0.2, 0.80)
        v_base = pf.random.log_uniform(rng, 0.01, 0.5)
        base_color_hsv = np.array([h_base, s_base, v_base])
        base_color = pf.color.hsv_to_rgba(base_color_hsv)
    else:
        h_base = pf.random.clip_gaussian(rng, 0.15, 0.1, 0.0, 1.0)
        s_base = pf.random.uniform(rng, 0.0, 0.80)
        v_base = pf.random.log_uniform(rng, 0.01, 0.5)
        base_color_hsv = np.array([h_base, s_base, v_base])

    if texture_color_layer1 is None:
        h_perturb = (pf.random.beta(rng, 0.8, 0.9) - 0.5) * 0.05
        s_perturb = pf.random.uniform(rng, -0.175, 0.175)
        v_mult = pf.random.log_uniform(rng, 0.5, 2.0)
        texture_color_layer1_hsv = np.array(
            [h_base + h_perturb, s_base + s_perturb, v_base * v_mult]
        )
        texture_color_layer1 = pf.color.hsv_to_rgba(texture_color_layer1_hsv)
    if size_layer1 is None:
        size_layer1 = pf.random.uniform(rng, 0.3, 2.5)
    if spread_layer1 is None:
        spread_layer1 = pf.random.uniform(rng, 0.1, 1.5)
    if smoothness_layer1 is None:
        smoothness_layer1 = pf.random.uniform(rng, 0.15, 0.95)

    color_layer_result = sandstone_color_layer(
        vector=vector,
        base_color=base_color,
        noise_type=2.0,
        texture_color=texture_color_layer1,
        mix_mode=1.0,
        size=size_layer1,
        stretch=1.0,
        detail=8.0,
        spread=spread_layer1,
        smoothness=smoothness_layer1,
    )

    if texture_color_layer2 is None:
        h_perturb = (pf.random.beta(rng, 0.3, 0.4) - 0.5) * 0.40
        s_perturb = pf.random.uniform(rng, -0.40, 0.40)
        v_mult = pf.random.log_uniform(rng, 0.15, 4.0)
        texture_color_layer2_hsv = np.array(
            [h_base + h_perturb, s_base + s_perturb, v_base * v_mult]
        )
        texture_color_layer2 = pf.color.hsv_to_rgba(texture_color_layer2_hsv)
    if size_layer2 is None:
        size_layer2 = pf.random.uniform(rng, 0.3, 2.5)
    if w_layer2 is None:
        w_layer2 = pf.random.uniform(rng, 0.0, 20.0)
    if spread_layer2 is None:
        spread_layer2 = pf.random.uniform(rng, 0.3, 1.0)
    if mask_layer2 is None:
        mask_layer2 = pf.random.uniform(rng, 0.2, 0.95)

    color_layer_result_1 = sandstone_color_layer(
        vector=color_layer_result.vector,
        base_color=color_layer_result.color,
        noise_type=2.0,
        texture_color=texture_color_layer2,
        mix_mode=1.0,
        size=size_layer2,
        stretch=1.0,
        w=w_layer2,
        detail=8.0,
        spread=spread_layer2,
        smoothness=1.0,
        mask=mask_layer2,
    )

    if texture_color_layer3 is None:
        h_perturb = (pf.random.beta(rng, 0.3, 0.4) - 0.5) * 0.40
        s_perturb = pf.random.uniform(rng, -0.35, 0.35)
        v_mult = pf.random.log_uniform(rng, 0.2, 3.0)
        texture_color_layer3_hsv = np.array(
            [h_base + h_perturb, s_base + s_perturb, v_base * v_mult]
        )
        texture_color_layer3 = pf.color.hsv_to_rgba(texture_color_layer3_hsv)
    if size_layer3 is None:
        size_layer3 = pf.random.uniform(rng, 0.02, 0.15)
    if spread_layer3 is None:
        spread_layer3 = pf.random.uniform(rng, 0.1, 1.0)
    if mask_layer3 is None:
        mask_layer3 = pf.random.uniform(rng, 0.10, 0.70)

    color_layer_result_2 = sandstone_color_layer(
        vector=color_layer_result_1.vector,
        base_color=color_layer_result_1.color,
        texture_color=texture_color_layer3,
        mix_mode=1.0,
        size=size_layer3,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer3,
        smoothness=1.0,
        mask=mask_layer3,
    )

    if texture_color_layer4 is None:
        h_perturb = (pf.random.beta(rng, 0.3, 0.4) - 0.5) * 0.55
        s_perturb = pf.random.uniform(rng, -0.275, 0.275)
        v_mult = pf.random.log_uniform(rng, 0.2, 5.0)
        texture_color_layer4_hsv = np.array(
            [h_base + h_perturb, s_base + s_perturb, v_base * v_mult]
        )
        texture_color_layer4 = pf.color.hsv_to_rgba(texture_color_layer4_hsv)
    if size_layer4 is None:
        size_layer4 = pf.random.uniform(rng, 0.3, 2.5)
    if spread_layer4 is None:
        spread_layer4 = pf.random.uniform(rng, 0.1, 1.2)

    color_layer_result_3 = sandstone_color_layer(
        vector=color_layer_result_2.vector,
        base_color=color_layer_result_2.color,
        texture_color=texture_color_layer4,
        mix_mode=2.0,
        size=size_layer4,
        stretch=1.0,
        detail=4.0,
        spread=spread_layer4,
        smoothness=1.0,
        mask=0.25,
    )

    if roughness is None:
        roughness = pf.random.uniform(rng, 0.02, 0.98)
    if specular_ior_level is None:
        specular_ior_level = pf.random.uniform(rng, 0.02, 0.08)

    surface = pf.nodes.shader.principled_bsdf(
        base_color=color_layer_result_3.color,
        roughness=roughness,
        subsurface_anisotropy=0.0,
        specular_ior_level=specular_ior_level,
    )

    if height_disp1 is None:
        height_disp1 = pf.random.uniform(rng, 0.05, 0.75)
    if size_disp1 is None:
        size_disp1 = pf.random.uniform(rng, 0.01, 0.36)
    if stretch_disp1 is None:
        stretch_disp1 = pf.random.uniform(rng, 0.5, 3.0)
    if spread_disp1 is None:
        spread_disp1 = pf.random.uniform(rng, 0.2, 1.5)
    if smoothness_disp1 is None:
        smoothness_disp1 = pf.random.uniform(rng, 0.05, 1.0)
    if distortion_strength_disp1 is None:
        distortion_strength_disp1 = pf.random.uniform(rng, 0.1, 3.6)

    displacement_layer_result = sandstone_displacement_layer(
        vector=vector,
        noise_type=2.0,
        height=height_disp1,
        mix_mode=1.0,
        size=size_disp1,
        stretch=stretch_disp1,
        rotation=(0.0, 0.0, 0.0),
        detail=6.0,
        spread=spread_disp1,
        smoothness=smoothness_disp1,
        distortion_strength=distortion_strength_disp1,
    )

    if height_disp2 is None:
        height_disp2 = pf.random.uniform(rng, 0.03, 0.53)
    if size_disp2 is None:
        size_disp2 = pf.random.uniform(rng, 0.01, 0.36)
    if stretch_disp2 is None:
        stretch_disp2 = pf.random.uniform(rng, 0.5, 2.5)
    if spread_disp2 is None:
        spread_disp2 = pf.random.uniform(rng, 0.2, 1.5)
    if smoothness_disp2 is None:
        smoothness_disp2 = pf.random.uniform(rng, 0.2, 1.0)
    if distortion_strength_disp2 is None:
        distortion_strength_disp2 = pf.random.uniform(rng, 0.15, 3.65)

    displacement_layer_result_1 = sandstone_displacement_layer(
        vector=displacement_layer_result.vector,
        base_height=displacement_layer_result.height,
        noise_type=2.0,
        height=height_disp2,
        mix_mode=1.0,
        size=size_disp2,
        stretch=stretch_disp2,
        rotation=(0.0, 0.0, 0.0),
        detail=8.0,
        spread=spread_disp2,
        smoothness=smoothness_disp2,
        distortion_strength=distortion_strength_disp2,
    )

    if height_disp3 is None:
        height_disp3 = pf.random.uniform(rng, 0.01, 0.26)
    if size_disp3 is None:
        size_disp3 = pf.random.uniform(rng, 0.001, 0.031)
    if spread_disp3 is None:
        spread_disp3 = pf.random.uniform(rng, 0.2, 1.5)
    if smoothness_disp3 is None:
        smoothness_disp3 = pf.random.uniform(rng, 0.2, 1.0)
    if distortion_strength_disp3 is None:
        distortion_strength_disp3 = pf.random.uniform(rng, 0.1, 0.9)

    displacement_layer_result_2 = sandstone_displacement_layer(
        vector=displacement_layer_result_1.vector,
        base_height=displacement_layer_result_1.height,
        noise_type=2.0,
        height=height_disp3,
        mix_mode=1.0,
        size=size_disp3,
        stretch=1.0,
        rotation=(0.0, 0.0, 0.0),
        detail=6.0,
        spread=spread_disp3,
        smoothness=smoothness_disp3,
        distortion_strength=distortion_strength_disp3,
    )

    displacement = pf.nodes.shader.displacement(
        height=displacement_layer_result_2.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )

    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def stone_smooth_presets(
    rng: pf.RNG,
    vector: t.SocketOrVal[pf.Vector],
) -> pf.Material:
    func = pf.control.choice(
        rng,
        [
            (_stone_sandstone_distribution, 1.0),
            (_stone_limestone_distribution, 1.0),
            (_stone_basalt_distribution, 1.0),
            (_stone_volcanic_distribution, 1.0),
            (_stone_tuff_distribution, 1.0),
            (_stone_grey_distribution, 1.0),
            (_stone_red_sandstone_distribution, 0.3),
        ],
    )
    return func(rng=rng, vector=vector)
