# Copyright (C) 2026, Princeton University.
# This source code is licensed under the BSD 3-Clause license found in the LICENSE file in the root directory of this source tree.

# Transpiled into procfunc/v2 format by Alexander Raistrick

import numpy as np
import procfunc as pf

from infinigen2.shaders.displacements import masonry
from infinigen2.shaders.masks import brick_selector, cracks
from infinigen2.shaders.masks.brick_cutter import (
    BrickCutterResult,
    brick_cutter,
    brick_cutter_rand,
    brick_dimensions_rand,
)
from infinigen2.shaders.masks.tile_shapes import tile_mask_rand
from infinigen2.shaders.materials import (
    brick_concrete,
    concrete,
    granite,
    marble,
    paint,
    stone_smooth,
)

__all__ = [
    "brick_color_pattern_rand",
    "brick_shader_composite_rand",
    "bricks_masonry_rand",
    "bricks_paint_rand",
    "bricks_presets",
    "bricks_pristine_rand",
    "bricks_rand",
]


def bricks_red_concave_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        gab=0.1765,
        round_corner=0.2717,
        warp_strength=1.0,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=0.1,
        brick_bevel=0.1621,
        brick_bevel_concavity=1.0,
        grout_extrusion=0.25,
        grout_bevel_concavity=1.0,
    )
    brick_concrete_result = brick_concrete.brick_concrete(
        vector=vector,
        color=pf.Color((0.217, 0.197, 0.194)),
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.6975,
        color_noise_1_size=0.04,
        color_noise_2=1.0,
        color_noise_2_size=0.0001,
        color_dots=1.0,
        color_dot_size=0.0003,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_size=0.1,
        displacement_noise_height=0.001,
        displacement_hole_depth=0.002,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface_color_1 = brick_cutter_result.random.astype(dtype=pf.Vector)
    surface_color = pf.nodes.color.mix_rgb(
        factor=1.0,
        a=pf.Color((0.577, 0.206, 0.132)),
        b=surface_color_1.x.astype(dtype=pf.Color),
        blend_type="SOFT_LIGHT",
    )

    brick_concrete_result_1 = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=surface_color,
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.3779,
        color_noise_1_size=0.02,
        color_noise_2=0.2409,
        color_noise_2_size=0.01,
        color_dots=0.5842,
        color_dot_size=0.0007,
        color_micro_grain_variation=0.411,
        color_grain_size=0.002,
        displacement_noise_height=0.001,
        displacement_hole_size=0.1,
        displacement_hole_depth=0.003,
        displacement_chip_size=1.5,
        displacement_chip_spread=0.9025,
        displacement_chip_depth=0.005,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.surface,
        b=brick_concrete_result_1.surface,
    )

    displacement = pf.nodes.math.mix(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.displacement,
        b=brick_concrete_result_1.displacement,
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def bricks_red_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        gab=0.1765,
        round_corner=0.2717,
        warp_strength=1.0,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=0.1,
        brick_bevel=0.1621,
        brick_bevel_concavity=1.0,
        grout_extrusion=0.25,
        grout_bevel_concavity=1.0,
    )
    brick_concrete_result = brick_concrete.brick_concrete(
        vector=vector,
        color=pf.Color((0.217, 0.197, 0.194)),
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.54,
        color_noise_1=0.6975,
        color_noise_1_size=0.04,
        color_noise_2=1.0,
        color_noise_2_size=0.0001,
        color_dots=1.0,
        color_dot_size=0.0003,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_size=0.1,
        displacement_noise_height=0.001,
        displacement_hole_depth=0.002,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface_color_1 = brick_cutter_result.random.astype(dtype=pf.Vector)
    surface_color = pf.nodes.color.mix_rgb(
        factor=1.0,
        a=pf.Color((0.577, 0.206, 0.132)),
        b=surface_color_1.x.astype(dtype=pf.Color),
        blend_type="SOFT_LIGHT",
    )

    brick_concrete_result_1 = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=surface_color,
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.3779,
        color_noise_1_size=0.02,
        color_noise_2=0.2409,
        color_noise_2_size=0.01,
        color_noise_2_spread=0.9,
        color_dots=0.5842,
        color_dot_size=0.0007,
        color_micro_grain_variation=0.411,
        color_grain_size=0.002,
        displacement_noise_height=0.001,
        displacement_hole_size=0.1,
        displacement_hole_depth=0.003,
        displacement_chip_depth=0.01,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.surface,
        b=brick_concrete_result_1.surface,
    )

    displacement = pf.nodes.math.mix(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.displacement,
        b=brick_concrete_result_1.displacement,
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def bricks_grey_rounded_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        brick_scaling=2.0,
        irregularity=0.3287,
        gab=0.15,
        round_corner=0.223,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=0.1,
        brick_bevel=0.1621,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-1.0,
        grout_bevel_concavity=1.0,
    )
    brick_concrete_result = brick_concrete.brick_concrete(
        color=pf.Color((0.093, 0.084, 0.084)),
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.6975,
        color_noise_1_size=0.04,
        color_noise_2=1.0,
        color_noise_2_size=0.0001,
        color_dots=1.0,
        color_dot_size=0.0003,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_size=0.1,
        displacement_noise_height=0.001,
        displacement_hole_depth=0.002,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface_color_factor = pf.nodes.math.constant(0.286)
    surface_color_2 = pf.nodes.math.constant(pf.Color((0.5, 0.5, 0.5)))
    surface_color_1 = brick_cutter_result.random.astype(dtype=pf.Vector)
    surface_color = pf.nodes.color.mix_rgb(
        factor=surface_color_factor,
        a=surface_color_2,
        b=surface_color_1.x.astype(dtype=pf.Color),
        blend_type="SOFT_LIGHT",
    )

    brick_concrete_result_1 = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=surface_color,
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        distortion_size=1.0,
        distortion_strength=0.1,
        color_noise_1=0.0,
        color_noise_1_size=0.02,
        color_noise_2=0.0,
        color_noise_2_size=0.01,
        color_dots=0.9269,
        color_dot_size=0.001,
        color_dot_spread=0.5,
        color_dot_sharpness=0.7329,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_height=0.001,
        displacement_hole_size=0.1,
        displacement_hole_depth=0.0,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.surface,
        b=brick_concrete_result_1.surface,
    )

    displacement = pf.nodes.math.mix(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.displacement,
        b=brick_concrete_result_1.displacement,
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def bricks_grey_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        irregularity=0.3287,
        gab=0.15,
        round_corner=0.223,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=0.1,
        brick_bevel=0.1621,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel=0.49,
        grout_bevel_concavity=1.0,
    )
    brick_concrete_result = brick_concrete.brick_concrete(
        vector=vector,
        color=pf.Color((0.093, 0.084, 0.084)),
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.6975,
        color_noise_1_size=0.04,
        color_noise_2=1.0,
        color_noise_2_size=0.0001,
        color_dots=1.0,
        color_dot_size=0.0003,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_size=0.1,
        displacement_noise_height=0.001,
        displacement_hole_depth=0.002,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface_color_factor = pf.nodes.math.constant(0.286)
    surface_color_1 = brick_cutter_result.random.astype(dtype=pf.Vector)
    surface_color = pf.nodes.color.mix_rgb(
        factor=surface_color_factor,
        a=pf.Color((0.391, 0.391, 0.391)),
        b=surface_color_1.x.astype(dtype=pf.Color),
        blend_type="SOFT_LIGHT",
    )

    brick_concrete_result_1 = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=surface_color,
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        distortion_size=1.0,
        distortion_strength=0.1,
        color_noise_1=0.0,
        color_noise_1_size=0.02,
        color_noise_2=0.0,
        color_noise_2_size=0.01,
        color_dots=0.9269,
        color_dot_size=0.001,
        color_dot_spread=0.5,
        color_dot_sharpness=0.7329,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_height=0.001,
        displacement_hole_size=0.1,
        displacement_hole_depth=0.0,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.surface,
        b=brick_concrete_result_1.surface,
    )

    displacement = pf.nodes.math.mix(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.displacement,
        b=brick_concrete_result_1.displacement,
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def bricks_cobble_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.07,
        gab=0.05,
        round_corner=0.05,
        warp_strength=0.4,
        brick_extrusion=0.1,
        brick_extrusion_variation=0.5,
        brick_bevel=0.01,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.48,
        grout_bevel_concavity=1.0,
    )

    surface_1 = pf.nodes.shader.diffuse_bsdf(
        color=pf.Color((0.042, 0.024, 0.02)),
        normal=(0.0, 0.0, 0.0),
    )

    granite_result = granite.granite_cobble_gray_preset(vector=vector)

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=surface_1,
        b=granite_result.surface,
    )

    displacement_1 = pf.nodes.shader.displacement(
        height=brick_cutter_result.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    displacement = granite_result.displacement + displacement_1
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def brick_masonry_sharp_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.35,
        brick_height=0.15,
        gab=0.025,
        warp_strength=1.0,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=1.0,
        brick_bevel=0.0,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel_concavity=1.0,
    )

    surface_1 = pf.nodes.shader.diffuse_bsdf(
        color=pf.Color((0.061, 0.06, 0.054)),
        normal=(0.0, 0.0, 0.0),
    )

    granite_result = granite.granite_masonry_gray_preset(
        vector=brick_cutter_result.vector
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=surface_1,
        b=granite_result.surface,
    )

    displacement_layer_1_height = brick_cutter_result.distance_from_edge**0.32
    displacement_layer_2_mask = pf.nodes.math.map_range(
        value=brick_cutter_result.distance_from_edge, from_max=0.43
    )

    layer_1_result = masonry.voronoi_displacement_layer(
        vector=brick_cutter_result.vector * (0.6, 1.0, 1.0),
        size=0.09,
        detail=0.0,
        warp_strength=2.0,
        warp_size=0.25,
        height=displacement_layer_1_height * 0.35,
        clamp_max=9.715,
    )
    layer_2_result = masonry.noise_displacement_layer(
        vector=layer_1_result.displacement,
        previous_layer=layer_1_result.height,
        mask=displacement_layer_2_mask,
        size=0.1,
        detail=4.0,
        warp_size=0.2,
        warp_detail=1.0,
        height=0.025,
        clamp_min=-12.0,
    )
    displacement = pf.nodes.shader.displacement(
        height=layer_2_result.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def brick_masonry_granite_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_height=0.18,
        gab=0.05,
        warp_strength=1.0,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=1.0,
        brick_bevel=0.0,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel_concavity=1.0,
    )

    surface_1 = pf.nodes.shader.diffuse_bsdf(
        color=pf.Color((0.141, 0.079, 0.061)),
        normal=(0.0, 0.0, 0.0),
    )

    granite_result = granite.granite_masonry_gray_preset(
        vector=brick_cutter_result.vector
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=surface_1,
        b=granite_result.surface,
    )

    displacement_layer_2_mask = pf.nodes.math.map_range(
        value=brick_cutter_result.distance_from_edge, from_max=0.43
    )

    layer_1_result = masonry.noise_displacement_layer(
        vector=brick_cutter_result.vector,
        size=0.1,
        height=brick_cutter_result.distance_from_edge * 0.2,
        clamp_max=0.015,
    )
    layer_2_result = masonry.noise_displacement_layer(
        vector=layer_1_result.displacement,
        previous_layer=layer_1_result.height,
        mask=displacement_layer_2_mask,
        size=0.2,
        detail=8.0,
        height=0.1,
        clamp_min=-12.0,
    )
    displacement = pf.nodes.shader.displacement(
        height=layer_2_result.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def brick_masonry_brown_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.35,
        brick_height=0.15,
        gab=0.025,
        warp_strength=1.0,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=1.0,
        brick_bevel=0.0,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel_concavity=1.0,
    )

    surface_1 = pf.nodes.shader.diffuse_bsdf(
        color=pf.Color((0.061, 0.06, 0.054)),
        normal=(0.0, 0.0, 0.0),
    )

    brick_concrete_result = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=pf.Color((0.294, 0.19, 0.126)),
        roughness=0.7,
        specular_ior_level=0.1,
        color_noise_1_spread=0.8,
        color_noise_2_sharpness=0.25,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=surface_1,
        b=brick_concrete_result.surface,
    )

    displacement_layer_2_mask = pf.nodes.math.map_range(
        value=brick_cutter_result.distance_from_edge, from_max=0.43
    )

    layer_1_result = masonry.voronoi_displacement_layer(
        vector=brick_cutter_result.vector,
        size=0.12,
        detail=0.0,
        warp_strength=2.0,
        warp_size=0.25,
        warp_detail=1.0,
        height=brick_cutter_result.distance_from_edge * 0.32,
        clamp_max=0.015,
    )
    layer_2_result = masonry.noise_displacement_layer(
        vector=layer_1_result.displacement,
        previous_layer=layer_1_result.height,
        mask=displacement_layer_2_mask,
        size=0.1,
        detail=4.0,
        warp_size=0.2,
        warp_detail=1.0,
        height=0.025,
        clamp_min=-12.0,
    )
    displacement = pf.nodes.shader.displacement(
        height=layer_2_result.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def bricks_checkered_sparse_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.2,
        brick_height=0.05,
        irregularity=0.3,
        gab=0.05,
        round_corner=0.05,
        warp_strength=0.4,
        brick_extrusion=1.0,
        brick_bevel=0.01,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel_concavity=1.0,
    )

    surface_1 = pf.nodes.shader.principled_bsdf(subsurface_anisotropy=0.0)

    brick_selector_result = brick_selector.brick_selector(
        brick_id=brick_cutter_result.brick_id, skip_x=1.0, skip_y=1.0, bias=1.0
    )

    displacement_color = pf.nodes.color.mix_rgb(
        factor=brick_selector_result.selection,
        a=pf.Color((0.06, 0.01, 0.006)),
        b=pf.Color((0.554, 0.338, 0.2)),
    )

    brick_concrete_result = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=displacement_color,
        roughness=0.9409,
        specular_ior_level=0.0753,
        color_noise_1_spread=0.8,
        color_noise_2=1.0,
        color_noise_2_size=0.8548,
        color_noise_2_sharpness=0.25,
        color_dots=0.3,
        displacement_noise_height=0.001,
        displacement_hole_size=0.0,
        displacement_hole_depth=0.0,
        displacement_chip_size=2.0,
        displacement_chip_spread=1.0,
        displacement_chip_depth=0.002,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=surface_1,
        b=brick_concrete_result.surface,
    )
    return pf.Material(
        surface=surface,
        displacement=brick_concrete_result.displacement,
    )


def bricks_checkered_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.2,
        brick_height=0.04,
        irregularity=0.4,
        gab=0.05,
        round_corner=0.05,
        warp_strength=0.4,
        brick_extrusion=0.2,
        brick_bevel=0.01,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel_concavity=1.0,
    )

    surface_1 = pf.nodes.shader.principled_bsdf(subsurface_anisotropy=0.0)

    brick_selector_result = brick_selector.brick_selector(
        brick_id=brick_cutter_result.brick_id,
        skip_x=0.0,
        skip_y=0.0,
        shift=0.0,
        bias=1.0,
    )

    displacement_color = pf.nodes.color.mix_rgb(
        factor=brick_selector_result.selection,
        a=pf.Color((0.06, 0.021, 0.018)),
        b=pf.Color((0.478, 0.301, 0.187)),
    )

    brick_concrete_result = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=displacement_color,
        roughness=0.9409,
        specular_ior_level=0.0753,
        color_noise_1_spread=0.8,
        color_noise_2=1.0,
        color_noise_2_size=0.8548,
        color_noise_2_sharpness=0.25,
        color_dots=0.3,
        displacement_noise_height=0.005,
        displacement_hole_size=0.0,
        displacement_chip_size=2.0,
        displacement_chip_spread=1.0,
        displacement_chip_depth=0.01,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=surface_1,
        b=brick_concrete_result.surface,
    )
    return pf.Material(
        surface=surface,
        displacement=brick_concrete_result.displacement,
    )


def bricks_striped_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.07,
        gab=0.05,
        round_corner=0.05,
        warp_strength=0.4,
        brick_extrusion=0.2,
        brick_bevel=0.01,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel_concavity=1.0,
    )

    surface_1 = pf.nodes.shader.principled_bsdf(subsurface_anisotropy=0.0)

    brick_selector_result = brick_selector.brick_selector(
        brick_id=brick_cutter_result.brick_id, skip_x=0.0, skip_y=3.0, shift=0.0
    )

    granite_result = granite.granite_sandy_preset(vector=brick_cutter_result.vector)

    displacement_color = pf.nodes.color.hue_saturation(
        hue=0.41,
        saturation=0.8,
        value=0.2,
        fac=brick_selector_result.selection,
        color=granite_result.color,
    )

    brick_concrete_result = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=displacement_color,
        roughness=0.9409,
        specular_ior_level=0.0753,
        color_noise_1_spread=0.8,
        color_noise_2=1.0,
        color_noise_2_size=0.8548,
        color_noise_2_sharpness=0.25,
        color_dots=0.3,
        displacement_hole_size=0.0,
        displacement_chip_size=2.0,
        displacement_chip_spread=0.7,
        displacement_chip_depth=0.005,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=surface_1,
        b=brick_concrete_result.surface,
    )
    return pf.Material(
        surface=surface,
        displacement=brick_concrete_result.displacement,
    )


def bricks_paint_strokes_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    paint_result = paint.paint(
        vector=vector,
        bump_height=0.003,
        bump_size=0.01,
        splat_height=0.003,
        splat_size=0.042,
        splat_width=0.4509,
        splat_warp_size=0.12,
        stroke_height=0.004,
        stroke_size=0.005,
        stroke_spread=0.8538,
        stroke_length=0.1,
        globule_height=0.0,
        globule_spread=0.5,
    )

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        brick_scaling=2.0,
        irregularity=0.3287,
        gab=0.15,
        round_corner=0.223,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=0.1,
        brick_bevel=0.33,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.2,
        grout_bevel_concavity=1.0,
    )
    brick_concrete_result = brick_concrete.brick_concrete(
        vector=vector,
        color=pf.Color((0.217, 0.197, 0.194)),
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.6975,
        color_noise_1_size=0.04,
        color_noise_2=1.0,
        color_noise_2_size=0.0001,
        color_dots=1.0,
        color_dot_size=0.0003,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_size=0.1,
        displacement_noise_height=0.001,
        displacement_hole_depth=0.002,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    displacement_color_1 = brick_cutter_result.random.astype(dtype=pf.Vector)
    displacement_color = pf.nodes.color.mix_rgb(
        factor=1.0,
        a=pf.Color((0.391, 0.391, 0.391)),
        b=displacement_color_1.x.astype(dtype=pf.Color),
        blend_type="SOFT_LIGHT",
    )

    brick_concrete_result_1 = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=displacement_color,
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        distortion_size=1.0,
        distortion_strength=0.1,
        color_noise_1=0.0,
        color_noise_1_size=0.02,
        color_noise_2=0.0,
        color_noise_2_size=0.01,
        color_dots=0.9269,
        color_dot_size=0.001,
        color_dot_spread=0.5,
        color_dot_sharpness=0.7329,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_height=0.001,
        displacement_hole_size=0.1,
        displacement_hole_depth=0.0,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    displacement_1 = pf.nodes.math.mix(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.displacement,
        b=brick_concrete_result_1.displacement,
    )
    displacement = paint_result.displacement + displacement_1
    return pf.Material(
        surface=paint_result.surface,
        displacement=displacement,
    )


def bricks_paint_spots_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    paint_result = paint.paint(
        vector=vector,
        roughness=0.0708,
        specular_ior_level=0.3208,
        bump_height=0.001,
        bump_size=0.04,
        splat_height=0.0,
        splat_size=0.042,
        splat_width=0.4509,
        splat_warp_size=0.12,
        stroke_height=0.0,
        stroke_size=0.005,
        stroke_spread=0.8538,
        stroke_length=0.1,
        globule_height=0.003,
        globule_size=0.02,
        globule_spread=0.5,
    )

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        irregularity=0.3287,
        gab=0.071,
        round_corner=0.223,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=0.1,
        brick_bevel=0.1621,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.2,
        grout_bevel_concavity=1.0,
    )
    brick_concrete_result = brick_concrete.brick_concrete(
        vector=vector,
        color=pf.Color((0.217, 0.197, 0.194)),
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.6975,
        color_noise_1_size=0.04,
        color_noise_2=1.0,
        color_noise_2_size=0.0001,
        color_dots=1.0,
        color_dot_size=0.0003,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_size=0.1,
        displacement_noise_height=0.001,
        displacement_hole_depth=0.002,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    displacement_color_1 = brick_cutter_result.random.astype(dtype=pf.Vector)
    displacement_color = pf.nodes.color.mix_rgb(
        factor=1.0,
        a=pf.Color((0.577, 0.206, 0.132)),
        b=displacement_color_1.x.astype(dtype=pf.Color),
        blend_type="SOFT_LIGHT",
    )

    brick_concrete_result_1 = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=displacement_color,
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        distortion_size=1.0,
        distortion_strength=0.1,
        color_noise_1=0.0,
        color_noise_1_size=0.02,
        color_noise_2=0.0,
        color_noise_2_size=0.01,
        color_dots=0.9269,
        color_dot_size=0.001,
        color_dot_spread=0.5,
        color_dot_sharpness=0.7329,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_height=0.001,
        displacement_hole_size=0.1,
        displacement_hole_depth=0.0,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    displacement_1 = pf.nodes.math.mix(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.displacement,
        b=brick_concrete_result_1.displacement,
    )
    displacement = paint_result.displacement + displacement_1
    return pf.Material(
        surface=paint_result.surface,
        displacement=displacement,
    )


def bricks_paint_worn_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        irregularity=0.3287,
        gab=0.15,
        round_corner=0.223,
        warp_size=2.0,
        brick_bevel=0.1621,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.3,
        grout_bevel_concavity=1.0,
    )
    brick_concrete_result = brick_concrete.brick_concrete(
        vector=vector,
        color=pf.Color((0.217, 0.197, 0.194)),
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        color_noise_1=0.6975,
        color_noise_1_size=0.04,
        color_noise_2=1.0,
        color_noise_2_size=0.0001,
        color_dots=1.0,
        color_dot_size=0.0003,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_size=0.1,
        displacement_noise_height=0.001,
        displacement_hole_depth=0.002,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    surface_color_1 = brick_cutter_result.random.astype(dtype=pf.Vector)
    surface_color = pf.nodes.color.mix_rgb(
        factor=1.0,
        a=pf.Color((0.041, 0.041, 0.041)),
        b=surface_color_1.x.astype(dtype=pf.Color),
        blend_type="SOFT_LIGHT",
    )

    brick_concrete_result_1 = brick_concrete.brick_concrete(
        vector=brick_cutter_result.vector,
        color=surface_color,
        roughness=0.847,
        specular_ior_level=0.1941,
        stretch_x=1.34,
        distortion_size=1.0,
        distortion_strength=0.1,
        color_noise_1=0.0,
        color_noise_1_size=0.02,
        color_noise_2=0.0,
        color_noise_2_size=0.01,
        color_dots=0.9269,
        color_dot_size=0.001,
        color_dot_spread=0.5,
        color_dot_sharpness=0.7329,
        color_micro_grain_variation=1.0,
        color_grain_size=0.001,
        displacement_noise_height=0.001,
        displacement_hole_size=0.1,
        displacement_hole_depth=0.0,
        displacement_chip_depth=0.0,
        displacement_additional_height=brick_cutter_result.height,
    )

    displacement_1 = pf.nodes.math.mix(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.displacement,
        b=brick_concrete_result_1.displacement,
    )

    paint_result = paint.paint(
        vector=vector,
        bump_height=0.001,
        bump_size=0.005,
        bump_warp_size=0.02,
        splat_height=0.0,
        splat_size=0.05,
        stroke_height=0.001,
        globule_height=0.0,
        globule_spread=0.5,
    )

    wear_and_tear_result = cracks.cracks_worn_preset(
        vector=vector,
        displacement_a=displacement_1,
        displacement_b=paint_result.displacement,
    )

    surface_1 = pf.nodes.shader.mix_shader(
        factor=brick_cutter_result.mask,
        a=brick_concrete_result.surface,
        b=brick_concrete_result_1.surface,
    )
    surface = pf.nodes.shader.mix_shader(
        factor=wear_and_tear_result,
        a=surface_1,
        b=paint_result.surface,
    )

    displacement = pf.nodes.math.mix(
        factor=wear_and_tear_result,
        a=(0.0, 0.0, 0.0),
        b=displacement_1,
    )
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def bricks_paint_bumpy_preset(
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    # Code generated by procfunc v0.3.0

    paint_result = paint.paint(
        vector=vector,
        roughness=0.5583,
        specular_ior_level=0.3495,
        bump_height=0.005,
        bump_size=0.1,
        splat_height=0.005,
        splat_size=0.1,
        splat_width=0.9335,
        splat_spread=0.7767,
        stroke_height=0.0,
        globule_height=0.0,
        globule_spread=0.5,
    )

    brick_cutter_result = brick_cutter(
        vector=vector,
        brick_width=0.15,
        brick_height=0.05,
        gab=0.1,
        round_corner=0.223,
        warp_size=2.0,
        brick_extrusion=0.1,
        brick_extrusion_variation=1.0,
        brick_bevel=0.1621,
        brick_bevel_concavity=-1.0,
        grout_extrusion=-0.1,
        grout_bevel_concavity=1.0,
    )

    displacement_1 = pf.nodes.shader.displacement(
        height=brick_cutter_result.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    displacement = paint_result.displacement + displacement_1
    return pf.Material(
        surface=paint_result.surface,
        displacement=displacement,
    )


def bricks_presets(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    func = pf.control.choice(
        rng,
        [
            (bricks_red_concave_preset, 1.0),
            (bricks_red_preset, 1.0),
            (bricks_grey_rounded_preset, 1.0),
            (bricks_grey_preset, 1.0),
            (bricks_cobble_preset, 1.0),
            (brick_masonry_sharp_preset, 1.0),
            (brick_masonry_granite_preset, 1.0),
            (brick_masonry_brown_preset, 1.0),
            (bricks_checkered_sparse_preset, 1.0),
            (bricks_checkered_preset, 1.0),
            (bricks_striped_preset, 1.0),
            (bricks_paint_strokes_preset, 1.0),
            (bricks_paint_spots_preset, 1.0),
            (bricks_paint_worn_preset, 1.0),
            (bricks_paint_bumpy_preset, 1.0),
        ],
    )
    return func(vector=vector)


def brick_color_pattern_rand(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
    brick_id: pf.ProcNode[pf.Vector],
    brick_random: pf.ProcNode[pf.Color],
):
    def selector_option(rs, vector, brick_id, **_):
        rs1, rs2, rs3 = rs.spawn(3)
        fac = brick_selector.brick_selector_rand(rng=rs1, brick_id=brick_id)
        color_1 = brick_concrete.brick_concrete_color_rand(rs2)
        color_2 = brick_concrete.brick_concrete_color_rand(rs3)
        return pf.nodes.color.mix_rgb(
            factor=fac.selection,
            a=color_1,
            b=color_2,
        )

    def mix_per_brick_option(ro, vector, brick_random, **_):
        ro1, ro2 = ro.spawn(2)
        return pf.nodes.color.mix_rgb(
            factor=brick_random.x,
            a=brick_concrete.brick_concrete_color_rand(ro1),
            b=brick_concrete.brick_concrete_color_rand(ro2),
            blend_type="SOFT_LIGHT",
        )

    def discrete_per_brick_option(ro, vector, brick_random, **_):
        ratio = pf.random.uniform(ro, 0.1, 0.9)
        return pf.nodes.color.mix_rgb(
            factor=brick_random.x > ratio,
            a=brick_concrete.brick_concrete_color_rand(ro),
            b=brick_concrete.brick_concrete_color_rand(ro),
            blend_type="SOFT_LIGHT",
        )

    def constant_option(rc, **_):
        return brick_concrete.brick_concrete_color_rand(rc)

    def tile_pattern_color_option(rt, brick_id, **_):
        rt2, rt3, rt4 = rt.spawn(3)
        # x/y scales normalize brick_id to metric space for ~1.5m tile repeat
        tile_vector = pf.nodes.math.combine_xyz(
            x=brick_id.x * 0.667, y=brick_id.y * 0.233
        )
        border = pf.random.uniform(rt2, 0.15, 0.25)
        tile_mask = tile_mask_rand(rt2, vector=tile_vector, border=border)
        color_1 = brick_concrete.brick_concrete_color_rand(rt3)
        color_2 = brick_concrete.brick_concrete_color_rand(rt4)
        tile_type = tile_mask.mask.astype(dtype=pf.Vector).x > 0.5
        return pf.nodes.color.mix_rgb(factor=tile_type, a=color_1, b=color_2)

    r1, r2 = rng.spawn(2)

    color_func = pf.control.choice(
        r1,
        [
            (selector_option, 2.0),  # has more diversity than the others, upweight it
            (constant_option, 1.0),
            (mix_per_brick_option, 1.0),
            (discrete_per_brick_option, 1.0),
            (tile_pattern_color_option, 2.0),
            # random_per_brick
        ],
    )
    return color_func(r2, vector=vector, brick_id=brick_id, brick_random=brick_random)


def brick_shader_composite_rand(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
    brick_id: pf.ProcNode[pf.Vector],
    brick_random: pf.ProcNode[pf.Color],
) -> pf.Material:
    """
    Chooses a brick material, and when possible chooses a per-brick color scheme, e.g random, checkered, striped, etc
    """

    with_color = pf.control.choice(
        rng,
        [
            # TODO: more materials with the `color` argument
            (brick_concrete.brick_concrete_rand, 2.0),
        ],
    )

    no_color = pf.control.choice(
        rng,
        [
            # TODO: more materials with no `color` argument
            (granite.granite_rand, 1.0),
            (stone_smooth.stone_smooth_rand, 0.5),
            (concrete.concrete_rand, 1.0),
        ],
    )

    color_pattern = brick_color_pattern_rand(rng, vector, brick_id, brick_random)
    mat_with_pattern = with_color(rng, vector, color=color_pattern)
    mat_no_pattern = no_color(rng, vector)
    return pf.control.choice(
        rng,
        [
            (mat_with_pattern, 2.0),
            (mat_no_pattern, 1.0),
        ],
    )


@pf.tracer.grammar
def bricks_rand(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
    brick_cutter: BrickCutterResult | None = None,
    brick_shader: pf.Material | None = None,
    grout_shader: pf.Material | None = None,
) -> pf.Material:
    r1, r2, r3 = rng.spawn(3)

    if brick_cutter is None:
        brick_cutter = brick_cutter_rand(r1, vector)

    if brick_shader is None:
        brick_shader = brick_shader_composite_rand(
            r2,
            vector=brick_cutter.vector,
            brick_id=brick_cutter.brick_id,
            brick_random=brick_cutter.random,
        )

    if grout_shader is None:
        grout_shader = brick_concrete.brick_concrete_grout_rand(r3, vector)

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter.mask,
        a=grout_shader.surface,
        b=brick_shader.surface,
    )
    displacement = pf.nodes.math.mix(
        factor=brick_cutter.mask,
        a=grout_shader.displacement,
        b=brick_shader.displacement,
    )

    cutter_displacement = pf.nodes.shader.displacement(
        height=brick_cutter.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    displacement = displacement + cutter_displacement
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


def brick_concrete_pristine(rng: pf.RNG, vector: pf.ProcNode[pf.Vector]) -> pf.Material:
    r1, r2 = rng.spawn(2)
    displacement = pf.random.uniform(r1, 0.1, 0.2)
    return brick_concrete.brick_concrete_rand(r2, vector, displacement_pct=displacement)


def bricks_pristine_rand(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    r1, r2, r3, r4 = rng.spawn(4)

    shader = pf.control.choice(
        r1,
        [
            (brick_concrete_pristine, 0.5),
            (concrete.concrete_rand, 0.5),
            (granite.granite_smooth_rand, 1.0),
            (marble.marble_rand, 1.5),
        ],
    )

    dimensions = brick_dimensions_rand(r1)
    dimensions = pf.nodes.math.vector_multiply(dimensions, (1, 2, 1))

    cutter = brick_cutter_rand(
        r2, vector, irregularity=0, warp_strength=pf.random.uniform(rng, 0.05, 0.3)
    )

    return bricks_rand(
        r3,
        vector,
        brick_cutter=cutter,
        brick_shader=shader(r2, cutter.vector),
        grout_shader=brick_concrete.brick_concrete_grout_rand(r4, vector),
    )


def bricks_masonry_rand(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
    brick_shader: pf.Material | None = None,
    grout_shader: pf.Material | None = None,
) -> pf.Material:
    """

    Args:
    ---
    """

    rng, r1, r2, r3, r4 = rng.spawn(5)

    r1_1, r1_2 = r1.spawn(2)
    dimensions = brick_dimensions_rand(
        r1_1,
        brick_width_mean=0.35,
        brick_aspect_mean=0.5,
    )
    brick_cutter = brick_cutter_rand(
        r1,
        vector,
        brick_dimensions=dimensions,
        gab=pf.random.uniform(rng, 0.025, 0.05),
    )

    if brick_shader is None:
        r2_1, r2_2 = r2.spawn(2)
        distrib = pf.control.choice(
            r2_1,
            [
                (brick_concrete.brick_concrete_rand, 0.2),
                (granite.granite_rand, 2.0),
                (marble.marble_rand, 0.5),
            ],
        )
        brick_shader = distrib(rng=r2_2, vector=vector)

    if grout_shader is None:
        # artist demos never used light-colored grout for masonry
        color_hsv = pf.random.uniform(
            rng,
            np.array([0.0, 0.11, 0.06]),
            np.array([0.05, 0.56, 0.14]),
        )
        grout_color = pf.color.hsv_to_rgba(color_hsv)

        grout_shader = brick_concrete.brick_concrete_grout_rand(
            r3, vector, color=grout_color
        )

    surface = pf.nodes.shader.mix_shader(
        factor=brick_cutter.mask,
        a=grout_shader.surface,
        b=brick_shader.surface,
    )
    displacement = pf.nodes.math.mix(
        factor=brick_cutter.mask,
        a=grout_shader.displacement,
        b=brick_shader.displacement,
    )

    displacement_layer_2_mask = pf.nodes.math.map_range(
        value=brick_cutter.distance_from_edge, from_max=0.43
    )

    dimensions_sep = pf.nodes.math.separate_xyz(vector=dimensions)

    mult = dimensions_sep.x * (3 * pf.random.clip_gaussian(rng, 1, 0.2, 0.7, 1.7))
    # exp = pf.random.uniform(rng, 0.3, 1)
    height = mult * (brick_cutter.distance_from_edge)  # **exp)
    clip = dimensions_sep.x * pf.random.uniform(rng, 0.03, 0.17)

    masonry_displacement_result = masonry.masonry_displacement_rand(
        rng=r4,
        vector=brick_cutter.vector,
        layer_1_height=height,
        layer_1_clamp_max=clip,
        layer_2_mask=displacement_layer_2_mask,
        layer_2_height=dimensions_sep.y,
    )

    displacement = masonry_displacement_result.displacement
    return pf.Material(
        surface=surface,
        displacement=displacement,
    )


"""
def bricks_paint_rand(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
) -> pf.Material:
    return pf.control.choice(
        rng,
        [
            (bricks_paint_bumpy_preset, 1.0),
            (bricks_paint_strokes_preset, 1.0),
            (bricks_paint_spots_preset, 1.0),
            (bricks_paint_worn_preset, 1.0),
        ],
    )(vector=vector)
"""


def bricks_paint_rand(
    rng: pf.RNG,
    vector: pf.ProcNode[pf.Vector],
    bricks: pf.Material | None = None,
    paint_material: pf.Material | None = None,
    brick_cutter: BrickCutterResult | None = None,
    mask: pf.ProcNode[float] | None = None,
) -> pf.Material:
    """
    Creates a material with bricks covered with paint, sometimes with cracks allowing the underlying bricks to show through

    Note: you could also just use `bricks_rand` then `paint_overlay_rand`, but this would miss a couple features:
    - some options for the brick material are large enough to cause `Cycles: out of SVM stack space` when using paint+mask
    - this function ensures that cracks in paint

    """

    r1, r2, r3, r4, r5 = rng.spawn(5)

    if brick_cutter is None:
        brick_cutter = brick_cutter_rand(
            r1,
            vector,
            grout_extrusion=0,
        )

    if bricks is None:
        bricks = brick_concrete.brick_concrete_rand(r2, brick_cutter.vector)

    if paint_material is None:
        # NOTE: using `vector` not `brick_cutter_result.vector`
        #   this means paint strokes are contigous between bricks
        #   (which is realistic unless they are painted BEFORE assembly)
        paint_material = paint.paint_rand(r3, vector=vector)

    cutter_displacement = pf.nodes.shader.displacement(
        height=brick_cutter.height,
        midlevel=0.0,
        normal=(0.0, 0.0, 0.0),
    )
    bricks_displacement = bricks.displacement + cutter_displacement
    bricks_and_paint_displacement = bricks_displacement + paint_material.displacement

    if mask is not None:
        mix_mask = mask
    else:
        mix_mask = cracks.cracks_rand(
            r4,
            vector,
            displacement_a=bricks_and_paint_displacement,
            displacement_b=paint_material.displacement,
            height_threshold=0.02,
        ).mask

    cracks_mix_surface = pf.nodes.shader.mix_shader(
        factor=mix_mask,
        a=bricks.surface,
        b=paint_material.surface,
    )
    cracks_mix_displacement = pf.nodes.math.mix(
        factor=mix_mask,
        a=bricks_displacement,
        b=bricks_and_paint_displacement,
    )

    paint_overlay_cracks = pf.Material(
        surface=cracks_mix_surface,  # cracks_mix_surface,
        displacement=cracks_mix_displacement,
    )
    paint_overlay_nomask = pf.Material(
        surface=paint_material.surface,
        displacement=bricks_and_paint_displacement,
    )

    if mask is not None:
        # the user provided a mask, so we have to mix it
        return paint_overlay_cracks
    else:
        # OPTIMIZATION: in the case where THIS FUNCTION chooses to use no mask,
        #   we can avoid using `cracks_mix_surface`, which results in a smaller/cheaper material
        return pf.control.choice(
            r5, [(paint_overlay_cracks, 1.0), (paint_overlay_nomask, 1.0)]
        )
